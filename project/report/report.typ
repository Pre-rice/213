// ─────────────────────────────────────────────────────────────────────────────
// 排版设置
// ─────────────────────────────────────────────────────────────────────────────
#set page(
  paper: "a4",
  margin: (top: 2.5cm, bottom: 2.5cm, left: 3.0cm, right: 2.5cm),
  numbering: "1",
)

// 正文：小四号宋体，单倍行距
#set text(
  font: ("WenQuanYi Micro Hei", "WenQuanYi Zen Hei", "Noto Serif CJK SC",
         "Source Han Serif SC", "SimSun"),
  size: 12pt,
  lang: "zh",
  region: "cn",
)

#set par(
  leading: 1.0em,
  spacing: 1.2em,
  justify: true,
)

// 黑体辅助命令
#let hei(content) = text(
  font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
         "Source Han Sans SC", "SimHei"),
  content,
)

// 一级标题：四号黑体，居中
#show heading.where(level: 1): it => {
  set align(center)
  set text(
    font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
           "Source Han Sans SC", "SimHei"),
    size: 14pt,
    weight: "bold",
  )
  v(1.0em)
  it.body
  v(0.5em)
}

// 二级标题：小四号黑体，左对齐
#show heading.where(level: 2): it => {
  set align(left)
  set text(
    font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
           "Source Han Sans SC", "SimHei"),
    size: 12pt,
    weight: "bold",
  )
  v(0.6em)
  it.body
  v(0.3em)
}

// 三级标题：小四号黑体，左对齐
#show heading.where(level: 3): it => {
  set align(left)
  set text(
    font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
           "Source Han Sans SC", "SimHei"),
    size: 12pt,
    weight: "bold",
  )
  v(0.4em)
  it.body
  v(0.2em)
}

// ─────────────────────────────────────────────────────────────────────────────
// 标题（三号黑体，居中）
// ─────────────────────────────────────────────────────────────────────────────
#align(center)[
  #text(
    font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
           "Source Han Sans SC", "SimHei"),
    size: 16pt,
    weight: "bold",
  )[基于委托簿微观结构的多因子动态集成股票收益率预测]
]

#v(1.2em)

// ─────────────────────────────────────────────────────────────────────────────
// 摘要
// ─────────────────────────────────────────────────────────────────────────────
#align(center)[
  #text(
    font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
           "Source Han Sans SC", "SimHei"),
    size: 14pt,
    weight: "bold",
  )[摘　要]
]

#v(0.5em)

本文针对A股市场Tick级别（500ms快照）数据的超短期收益率预测问题，构建了一套基于委托簿微观结构的多因子动态集成预测系统。研究使用同一板块五只股票（A/B/C/D/E）的五日历史Tick数据，任务是在线逐Tick预测股票E未来五分钟中间价收益率（Return5min）。

在特征工程方面，从原始33个字段出发，提取了59个多维度因子，涵盖成交量失衡（TI）脉冲、委托量失衡（OVI）脉冲、深层委托簿压力（OBI）、大单成交失衡、累计成交流量、截面反转信号及多重非线性交互特征，并结合板块联动效应引入7个截面因子，利用日内时间特征区分上下午市场状态。在模型方面，采用28个Ridge回归子模型（正则化系数$alpha$在10至1000之间），配合基于滚动IC的Softmax动态集成算法，实现跨日自适应权重分配。该集成机制引入EWMA平滑（$beta$=0.007）、定频权重更新（每15个Tick）以及niche模型零预热机制，在有效抑制权重噪声的同时保证各盲测日上的集成增益均为正。

以严格的五折按日交叉验证（Leave-One-Day-Out，LODO）为标准，经过20轮迭代优化——包括特征工程扩展（Iter0→Iter19，IC从0.147提升至0.313）与反过拟合剪枝（Iter19→Iter20，模型数54→28，IC跃升至0.330，ICIR从7.82大幅提升至12.59）——最终模型五折CV均值IC达到0.3302，ICIR=12.59，相对初始基线（IC=0.147）提升124.6%。嵌套盲测验证证实无代码层面数据泄露，动态集成在所有五天上均优于稳定等权基准，平均增益约+0.051。

#v(0.5em)
#hei[关键词：]股票收益率预测；委托簿微观结构；动态集成；Ridge回归；滚动IC加权；Leave-One-Day-Out交叉验证

#pagebreak()

// ─────────────────────────────────────────────────────────────────────────────
// 一、模型介绍
// ─────────────────────────────────────────────────────────────────────────────
= 模型介绍

== 1.1　问题描述

本研究基于A股市场某板块五只股票（以A、B、C、D、E匿名）的Tick级快照数据，目标是在线逐Tick预测股票E的未来五分钟中间价收益率。具体定义如下：

- #hei[标签（label）：]$"Return5min"(t) = ("MidPrice"(t+600) - "MidPrice"(t)) / "MidPrice"(t)$，其中$"MidPrice" = ("BidPrice"_1 + "AskPrice"_1) / 2$，600个Tick约等于5分钟。
- #hei[数据规模：]五天历史数据，每天27,601行；每只股票33个原始字段，涵盖五档买卖报价量、成交信息及订单流信息。
- #hei[在线预测约束：]预测时只能使用$t$时刻及之前的已知信息，不能引入任何未来信息（无前视偏差）。
- #hei[评价指标：]IC（皮尔森相关系数，预测值与真实值）、ICIR（IC均值除以IC标准差）。

== 1.2　整体框架

如图1所示，整体框架分为离线训练和在线推理两阶段。

#figure(
  image("figures/fig1_ic_evolution.png", width: 95%),
  caption: [模型IC迭代演进曲线（Iter0→Iter20）],
) <fig1>

#hei[离线训练阶段]（`train_model.py`）：以五日全量数据为训练集，采用五折按日交叉验证评估，每折保留一天为盲测集、其余四天为训练集。训练内容包括：(1) 计算59个特征；(2) 训练28个Ridge子模型；(3) 无需预训练集成权重（权重在在线推理时实时计算）。训练完成后将28个模型的系数向量保存至`models.pkl`。

#hei[在线推理阶段]（`MyModel.py`）：每当收到新的Tick数据（股票E的一行 + 板块A/B/C/D各一行），执行如下步骤：(1) 更新增量式SMA/EMA滑动窗口状态；(2) 计算59个特征的当前值；(3) 对28个子模型进行线性推理（$hat(y)_i = bold(w)_i^top bold(x) + b_i$）；(4) 用滚动IC-Softmax动态集成得到最终预测值。

== 1.3　动态集成机制

动态集成的核心是：根据每个子模型在过去600个Tick（约5分钟）的滚动IC表现，动态调整其集成权重，使历史表现好的模型获得更高的权重。

具体算法如下：设$"IC"_i(t)$为第$i$个子模型在$[t - "window", t - "delay")$窗口内预测值与真实值的皮尔森相关系数，经EWMA平滑后得到$tilde("IC")_i(t)$。集成权重通过Softmax计算：

$w_i(t) = "softmax"(tilde("IC")_i(t) times "temp")_i$

其中$"temp"=17$为温度系数（控制权重集中程度），$"delay"=600$为标签滞后（确保集成时真实Return5min已知）。为防止权重崩塌，对所有权重施加下界$"floor"=0$，且稳定模型（MTC）在预热期内享有更高的先验权重$"stable\_prior"=2.0$。

权重每15个Tick更新一次（`update_freq=15`），EWMA平滑系数$beta=0.007$，niche模型在窗口期不足时权重从零开始（零预热机制）。

子模型分为两类：
- #hei[稳定模型（Stable）：]1个，MTC（基于TI纯净特征，全时段稳定），负责预热期兜底。
- #hei[Niche模型：]27个Ridge子模型，每个模型针对特定信号组合和时间段（上午/全天/下午），在特定市场条件下表现突出；通过集成机制互相弥补弱点。

// ─────────────────────────────────────────────────────────────────────────────
// 二、测试流程
// ─────────────────────────────────────────────────────────────────────────────
= 测试流程

== 2.1　特征和标签选取

=== 2.1.1　标签选取

预测目标为股票E未来五分钟中间价对数收益率的线性近似：

$y(t) = "Return5min"(t) = ("MidPrice"(t+600) - "MidPrice"(t)) / "MidPrice"(t)$

$t+600$时刻的中间价（即600个Tick后）在$t$时刻是未知的，因此模型在$t$时刻输出预测值，训练时使用真实值计算损失。对标签不做任何平滑或变换，保持原始收益率形式以便IC评估。

=== 2.1.2　特征选取原则

特征选取遵循以下原则：(1) #hei[无前视偏差]——所有特征仅使用$t$时刻及之前的数据；(2) #hei[全日IC方向一致]——候选特征须在全部5个测试日上IC符号一致（正向预测力稳定）；(3) #hei[正交互补]——新增特征须与现有特征低相关（相关系数$<0.7$），提供独立信息维度；(4) #hei[极端值处理]——对收益率类特征实施剪裁（长窗口$|x| <= 0.1$，短窗口$|x| <= 0.05$）。

=== 2.1.3　特征体系概览

最终共59个特征，分为8大类，如图2所示：

#figure(
  image("figures/fig3_feature_categories.png", width: 80%),
  caption: [特征体系分类分布（共59个特征）],
) <fig3>

各类特征简述如下：

(1) #hei[成交量失衡（TI）脉冲（8个）：]以SMA差分形式捕捉成交买卖方向的短期偏离，包括$"TI\_p"k = "SMA"_k ("TI") - "SMA"_{600}("TI")$（$k in {15,30,40,60}$）及EMA版本。

(2) #hei[委托量失衡（OVI）脉冲（5个）：]$"OVI"_t = (V_"bid" - V_"ask") / (V_"bid" + V_"ask" + epsilon)$，反映市场挂单方向动能，是最强的单因子信号（均值IC≈0.26）。

(3) #hei[委托/成交笔数失衡（4个）：]ONI（Order Number Imbalance）和TNI（Trade Number Imbalance）提供与OVI正交的委托数量维度信息。

(4) #hei[板块联动因子（7个）：]A/B/C/D四只股票的截面均值信号，包括板块OBI、板块TI脉冲、板块OVI脉冲等，捕捉系统性板块动量。

(5) #hei[日内时间特征（2个）：]`aft_13800`（下午标志）和`aft_12000`（稍早分界），捕捉日内时段系统性效应（上午动量、下午反转）。

(6) #hei[滞后价格收益特征（12个）：]包括$E$股和板块过去5分钟已实现收益（均值回复信号），以及$E$股不同时间尺度（30/60/120/300/600/900 Tick）的中间价收益率。

(7) #hei[委托簿深度与流量（7个）：]深层委托簿失衡（2-5档OBI）、买卖价差脉冲、大单成交失衡、日内累计成交流量失衡等，捕捉"耐心资金"和机构行为。

(8) #hei[交互与高级信号（14个）：]包括波动率条件化OVI、截面特异性OVI、价格动量加速度、多重两两交互特征（OVI×|收益率|、OVI×书深、OVI×板块收益等）及非线性变换（OVI平方项、价差加权OVI等），为Ridge线性模型提供非线性信息。

== 2.2　特征预处理

=== 2.2.1　在线增量计算

所有特征均通过增量式数据结构实时计算，避免历史数据全量重算：

- `_RunSMA(window)`：固定窗口滑动均值，使用双端队列（`deque`）维护窗口，$O(1)$更新。
- `_RunEMA(span)`：指数移动平均，$"EMA"_t = (1-alpha) dot "EMA"_{t-1} + alpha dot x_t$，其中$alpha = 2\/("span"+1)$。

=== 2.2.2　冷启动处理

日初前$k$个Tick（$k$为特征滞后长度）无历史数据，使用当日有效值的运行均值填充，而非用$0$填充。用$0$填充会产生"无信号"的先验，在冷启动期制造可学习但不泛化的假信号；用当日均值填充（中性填充）可避免模型在冷启动期学到虚假规律 @fillna。

=== 2.2.3　标准化

Ridge回归对特征量纲敏感。离线训练时，各子模型在训练集上用$z$-score标准化（均值归零、方差归一）；在线推理时，使用训练阶段保存的均值和标准差进行同等变换，由`models.pkl`随模型系数一同存储。

=== 2.2.4　极端值裁剪

价格收益率类特征在极端市场条件下可能出现异常大值，影响模型稳定性。对收益率类特征实施剪裁：

对长窗口收益率（$>= 120$ Tick）：$x arrow.l "clip"(x, -0.1, 0.1)$
- 短窗口收益率（30/60 Tick）：$x arrow.l "clip"(x, -0.05, 0.05)$

交互特征（乘积项）额外乘以归一化系数（如20或5）并裁剪至$[-0.5, 0.5]$，防止乘积项方差过大。

== 2.3　训练集和交叉验证集的设置

=== 2.3.1　五折按日交叉验证（LODO-CV）

由于训练数据仅有5天（Fold数等于日数），采用Leave-One-Day-Out交叉验证（LODO-CV）：每折将一天作为测试集，其余四天作为训练集，共5折。该方案具有如下优点：

- #hei[最大化训练数据利用率：]每折使用4天训练，贴近最终全量训练。
- #hei[测试日相互独立：]不同折的测试日无交叉，IC均值为5天的无偏估计。
- #hei[对齐竞赛评分机制：]竞赛评分按天取IC均值，LODO-CV与之完全一致。

在`GroupKFold(n_splits=5)`实现中，以`Day`列为分组标识，确保同一天的数据不会同时出现在训练集和测试集。分折结果：{Fold1→Day5, Fold2→Day4, Fold3→Day3, Fold4→Day2, Fold5→Day1}。

=== 2.3.2　嵌套盲测验证

为评估调参过拟合程度，还实施了嵌套盲测（Nested Blind Test）：

- #hei[外层（盲测）：]逐天保留为完全盲测集，用其余4天训练，在盲测天预测，得到"外层IC"。等价于LODO-CV，确认代码层面无数据泄露。
- #hei[内层（调参参考）：]在4天训练集内部运行4折CV，得到"内层IC"。内层IC是研究者调参时实际参考的数字，包含对4天数据的过拟合。
- #hei[内-外gap：]内层IC - 外层IC = 调参偏乐观程度。Iter20最终模型的内外gap约为$-0.019$（外层略高，属合理范围），证明调参过拟合可控。

== 2.4　模型训练与参数设定

=== 2.4.1　Ridge回归子模型

每个子模型为带$L_2$正则化的线性回归：

$hat(bold(y)) = bold(X) bold(w) + b, quad min_(bold(w),b) ||bold(y) - bold(X)bold(w) - b||_2^2 + alpha ||bold(w)||_2^2$

正则化系数$alpha$按模型特性设定：多数模型$alpha=150$（综合特征集），OVI纯净模型$alpha=10$（低维，需弱正则），高维交互特征模型$alpha=1000$（防过拟合）。Ridge回归相比OLS可有效防止高维共线特征引发系数爆炸，相比LightGBM在样本量仅5天（约$138000$行）时泛化性更强 @ridge。

=== 2.4.2　28个子模型的设计

如表1所示，28个子模型覆盖了多种特征子集和时间段组合。1个稳定模型（MTC）负责全时段稳健预测，作为预热期基准；27个niche模型针对特定信号组合（OVI纯净、TI纯净、截面反转、非线性变换等）和时段（`aft_12000`标志区分上午/下午），在各自擅长的市场条件下表现突出。

#figure(
  kind: table,
  caption: [子模型分类概览],
  table(
    columns: (auto, auto, auto, auto),
    align: (left, center, center, left),
    stroke: 0.5pt,
    [#hei[模型类别]], [#hei[数量]], [#hei[正则化$alpha$]], [#hei[特征重点]],
    [稳定（Stable）], [1], [150], [TI纯净 + 时间特征（MTC）],
    [OVI纯净niche], [4], [10–150], [OVI脉冲系列为主要输入],
    [截面反转niche], [3], [150], [sect\_ret\_lag + e\_ret\_lag + e\_sect\_lag\_gap],
    [非线性niche], [4], [150–1000], [spread\_wt\_ovi, ovi\_sq, 交互特征族],
    [深度/流量niche], [6], [150], [obi\_deep\_p15, cum\_flow\_imb, book\_pres\_pulse],
    [综合多因子niche], [10], [150–500], [多信号组合 + Huber损失变体],
  )
) <tab1>

=== 2.4.3　迭代优化历程

如图1所示，模型经历了20轮迭代优化，主要里程碑包括：

- #hei[Iter0–2（基础建设）：]建立严格LODO-CV评估体系，IC从0.147提升至0.224（+52%）。
- #hei[Iter3–8（集成引入）：]设计动态集成机制，引入日内时间特征和滞后收益特征，IC提升至0.279。
- #hei[Iter9–12（突破0.30）：]引入累计成交流量失衡和5个交互特征，配合IXN niche模型族，IC首次突破0.30，达到0.300。
- #hei[Iter13–19（精细优化）：]逐步添加动量加速度、波动率条件化、截面反转等高级信号，IC稳步提升至0.313，ICIR达7.82。
- #hei[Iter20（反过拟合剪枝）：]采用Leave-One-Model-Out（LOO）剪枝分析，将54个模型精简为28个，剔除冗余/有害模型。剪枝后IC跃升至0.330，ICIR从7.82飙升至12.59（+61%），证明过度复杂的集成会因子模型相互稀释而降低IC，适度剪枝可同时提升IC和跨日稳定性。

=== 2.4.4　集成参数设定

#figure(
  kind: table,
  caption: [动态集成参数汇总],
  table(
    columns: (auto, auto, auto),
    align: (left, center, left),
    stroke: 0.5pt,
    [#hei[参数]], [#hei[值]], [#hei[含义]],
    [`ENSEMBLE_WINDOW`], [600], [滚动IC计算窗口（约5分钟）],
    [`ENSEMBLE_TEMP`], [17], [Softmax温度（权重集中程度）],
    [`ENSEMBLE_FLOOR`], [0], [权重下界（允许权重降至0）],
    [`RETURN_DELAY`], [600], [IC计算所需标签滞后（Tick）],
    [`ENSEMBLE_UPDATE_FREQ`], [15], [权重更新频率（每15 Tick）],
    [`ENSEMBLE_EWMA_BETA`], [0.007], [IC平滑系数（长记忆，约143步半衰期）],
    [`STABLE_PRIOR`], [2.0], [稳定模型预热期先验权重倍数],
    [`NICHE_INIT_WEIGHT`], [0], [niche模型预热期初始权重],
  )
) <tab2>

== 2.5　测试结果展示与模型评价

=== 2.5.1　逐日IC结果

图3展示了最终模型（Iter20）的五折CV逐日IC。五天测试IC分别为：Day1=0.326、Day2=0.363、Day3=0.338、Day4=0.284、Day5=0.341，均值IC=0.330，ICIR=12.59。

#figure(
  image("figures/fig2_final_cv.png", width: 85%),
  caption: [Iter20最终模型五折CV逐日IC（ICIR=12.59）],
) <fig2>

Day2的IC（0.363）最高，Day4的IC（0.284）最低。Day4是典型的低波动趋势日，微观结构信号在此类日期的预测力相对弱——这也是为什么所有模型在Day4的IC普遍偏低。五天IC的标准差仅0.026，体现了极高的跨日稳定性（ICIR=12.59远超同类模型）。

=== 2.5.2　基线对比

图4展示了从Iter1（6特征线性基线）到Iter20（28模型动态集成）的逐日IC提升对比：

#figure(
  image("figures/fig4_iter_comparison.png", width: 90%),
  caption: [Iter1基线 vs Iter20最终模型逐日IC对比],
) <fig4>

相比基线，Iter20在每一天上均取得显著提升：Day1提升72.5%（0.189→0.326），Day3提升134.7%（0.144→0.338），Day4提升129.0%（0.124→0.284）——弱信号日（Day1/3/4）的改善尤为突出，体现了多因子集成对多样化市场状态的覆盖能力。

=== 2.5.3　动态集成增益验证

图5展示了嵌套盲测（Iter19，Iter20机制相同）中动态集成相对稳定等权基准的增益：

#figure(
  image("figures/fig5_ensemble_gain.png", width: 95%),
  caption: [动态集成 vs 稳定等权基准的IC增益（嵌套盲测，Iter19）],
) <fig5>

动态集成在所有5天上均优于稳定等权基准，平均增益+0.051，验证了滚动IC-Softmax机制的有效性。增益最大的是Day5（+0.104），该天niche模型的滚动IC高度分化，动态权重可精准识别强信号模型。

=== 2.5.4　综合模型评价

#figure(
  kind: table,
  caption: [最终模型（Iter20）综合评价指标],
  table(
    columns: (auto, auto, auto),
    align: (left, center, left),
    stroke: 0.5pt,
    [#hei[评价维度]], [#hei[指标值]], [#hei[说明]],
    [五折CV均值IC], [0.3302], [主要竞赛指标，out-of-sample估计],
    [五折CV IC标准差], [0.026], [跨日稳定性，越低越好],
    [ICIR（IC/Std）], [12.59], [风险调整后IC，历史最高],
    [Penalized（M−0.5S）], [0.317], [惩罚性指标，兼顾均值与方差],
    [内层4折IC], [0.311], [调参参考值（含轻微调参偏差）],
    [内-外gap], [−0.019], [负值=外层略高，调参过拟合可控],
    [集成增益（vs等权）], [+0.051], [动态集成相对等权基准提升],
    [模型复杂度], [28子模型，59特征], [相比Iter19（54×59）更简洁],
  )
) <tab3>

#hei[模型优点：]#[(1)] 特征工程覆盖多维度微观结构信息，每类特征提供独立信息维度；(2) 动态集成机制自适应识别不同日期的强信号，无需人工选择模型；(3) LOO剪枝有效解决集成过度复杂的过拟合问题，模型简洁度与预测力同步提升；(4) 严格LODO-CV和嵌套盲测双重验证，评估结果可信度高。

#hei[模型局限：]#[(1)] 数据仅5天，样本量有限，模型评估的统计置信度存在不确定性；(2) Day4（低波动趋势日）IC偏低（0.284），微观结构信号在趋势性行情中预测力相对弱；(3) 部分niche模型的信号在不同市场状态下（高/低波动率、趋势/震荡）表现分化较大，鲁棒性有待在更长时间序列上验证。

// ─────────────────────────────────────────────────────────────────────────────
// 参考文献
// ─────────────────────────────────────────────────────────────────────────────
= 参考文献

#bibliography("references.bib", style: "gb-7714-2015-numeric", title: none)
