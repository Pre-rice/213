// ─────────────────────────────────────────────────────────────────────────────
// 排版设置
// ─────────────────────────────────────────────────────────────────────────────
#set page(
  paper: "a4",
  margin: (top: 2.5cm, bottom: 2.5cm, left: 3.0cm, right: 2.5cm),
  numbering: "1",
)

// 正文：小四号宋体，单倍行距
#set text(
  font: ("WenQuanYi Micro Hei", "WenQuanYi Zen Hei", "Noto Serif CJK SC",
         "Source Han Serif SC", "SimSun"),
  size: 12pt,
  lang: "zh",
  region: "cn",
)

#set par(
  leading: 1.0em,
  spacing: 1.2em,
  justify: true,
)

// 黑体辅助命令
#let hei(content) = text(
  font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
         "Source Han Sans SC", "SimHei"),
  content,
)

// 一级标题：四号黑体，居中
#show heading.where(level: 1): it => {
  set align(center)
  set text(
    font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
           "Source Han Sans SC", "SimHei"),
    size: 14pt,
    weight: "bold",
  )
  v(1.0em)
  it.body
  v(0.5em)
}

// 二级标题：小四号黑体，左对齐
#show heading.where(level: 2): it => {
  set align(left)
  set text(
    font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
           "Source Han Sans SC", "SimHei"),
    size: 12pt,
    weight: "bold",
  )
  v(0.6em)
  it.body
  v(0.3em)
}

// 三级标题：小四号黑体，左对齐
#show heading.where(level: 3): it => {
  set align(left)
  set text(
    font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
           "Source Han Sans SC", "SimHei"),
    size: 12pt,
    weight: "bold",
  )
  v(0.4em)
  it.body
  v(0.2em)
}

// ─────────────────────────────────────────────────────────────────────────────
// 标题（三号黑体，居中）
// ─────────────────────────────────────────────────────────────────────────────
#align(center)[
  #text(
    font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
           "Source Han Sans SC", "SimHei"),
    size: 16pt,
    weight: "bold",
  )[基于委托簿微观结构的多因子动态集成股票收益率预测]
]

#v(1.2em)

// ─────────────────────────────────────────────────────────────────────────────
// 摘要
// ─────────────────────────────────────────────────────────────────────────────
#align(center)[
  #text(
    font: ("WenQuanYi Zen Hei", "WenQuanYi Micro Hei", "Noto Sans CJK SC",
           "Source Han Sans SC", "SimHei"),
    size: 14pt,
    weight: "bold",
  )[摘　要]
]

#v(0.5em)

本文针对A股市场Tick级别（500ms快照）数据的超短期收益率预测问题，以市场微观结构理论为数学原型，构建了一套多因子动态集成预测系统。研究使用同一板块五只股票（A/B/C/D/E）的五日历史Tick数据，任务是在线逐Tick预测股票E未来五分钟中间价收益率（Return5min）。

在特征工程方面，以Kyle（1985）连续拍卖模型和Glosten-Milgrom（1985）逆向选择模型为理论依据，从33个原始字段提取59个多维度因子，涵盖成交量失衡（TI）脉冲、委托量失衡（OVI）脉冲、深层委托簿压力（OBI）、大单成交失衡、截面反转及多重非线性交互特征。在模型方面，采用28个Ridge回归子模型（$alpha in [10, 1000]$），配合基于EWMA平滑滚动IC的Softmax动态集成算法，实现跨日自适应权重分配。

在方法创新方面，本文提出Leave-One-Model-Out（LOO）反过拟合剪枝算法，将子模型数量从54精简至28，IC从0.313升至0.330，ICIR从7.82大幅跃升至12.59（+61%）；同时引入稳定-Niche双层预热架构和板块截面异质信号，显著提升集成稳定性。以严格的五折按日交叉验证（LODO-CV）为准则，经20轮迭代优化，最终模型五折CV均值IC=0.3302，ICIR=12.59，相对初始基线（IC=0.147）提升124.6%。嵌套盲测验证证实无数据泄露，动态集成在所有五天均优于等权基准，平均增益约+0.051。

#v(0.5em)
#hei[关键词：]股票收益率预测；委托簿微观结构；动态集成；Ridge回归；滚动IC加权；Leave-One-Day-Out交叉验证

#pagebreak()

// ─────────────────────────────────────────────────────────────────────────────
// 一、引言
// ─────────────────────────────────────────────────────────────────────────────
= 一、引言

== 1.1　研究背景

随着算法交易和高频交易的兴起，对委托簿（Limit Order Book, LOB）微观结构信号的提取与建模已成为量化金融研究的核心课题之一。Tick级别数据以500毫秒为间隔记录市场的完整状态演化，蕴含着传统日频数据无法捕捉的价格发现信息。本文的研究背景来自典型的实盘量化交易场景：如何利用某板块多只股票的500ms Tick快照数据，实时预测目标股票E在未来5分钟内的中间价收益率，为超短期交易策略提供方向性Alpha信号。

这一问题的实际意义在于：(1) 高频预测信号是量化做市、统计套利和高频方向性交易的共同基础；(2) 在线推理约束（每收到一个Tick必须立即输出，无延迟）与离线回测有本质区别，工程实现难度更高；(3) 多只股票板块数据的联合利用（截面信号）是本文区别于单标的预测研究的重要特色。

== 1.2　数学模型的原型与来源

本研究的核心数学原型植根于#hei[市场微观结构理论]（Market Microstructure Theory），具体有以下三条理论脉络作为模型来源 @microstructure。

#hei[（1）Kyle（1985）连续拍卖模型 @kyle1985] 是本文特征工程的直接理论来源。该模型在均衡状态下证明，做市商根据观察到的净订单流 $u_t$（知情交易者与噪音交易者的合并买卖需求）调整报价，价格更新满足线性关系：

$ Delta m(t) = lambda dot u_t + epsilon_t $

其中 $lambda > 0$ 为Kyle's Lambda（价格冲击系数），$epsilon_t$ 为随机噪声。此结论直接启示构造成交量失衡（TI）和委托量失衡（OVI）作为预测因子——订单流方向与强度是价格走势的因果前驱。Cont等（2014）@ofi 进一步实证证明了订单流失衡（OFI）与中间价变动的统计显著线性关系。

#hei[（2）Glosten-Milgrom（1985）逆向选择模型 @glosten1985] 为深层委托簿特征提供理论依据。该模型指出：委托簿各档挂单量反映了做市商对逆向选择风险的对冲需求——深层买卖挂单量的不对称意味着对未来价格方向的隐含预期。这是我们引入深层OBI（2–5档委托量失衡）因子的经济学基础。

#hei[（3）Lo（2004）自适应市场假说（AMH）@amh] 解释了为何需要动态集成机制。AMH指出：市场效率并非固定常数，各类信号的有效性随市场参与者的竞争与学习不断演化。因此，任何静态单一因子模型均难以在多样化的市场状态（高/低波动率、趋势/震荡）下保持稳定预测力；必须通过滚动权重机制动态响应市场状态的切换。

== 1.3　主要贡献

本文的主要贡献体现在以下四个方面：

(1) 系统性地构建了基于微观结构理论的59因子特征体系，每类因子均有明确的经济学解释，覆盖成交、委托、跨资产截面和非线性交互四大维度。

(2) 提出Leave-One-Model-Out（LOO）反过拟合剪枝算法，首次在集成学习层面直接度量每个子模型对集成性能的边际贡献，实现模型压缩与性能提升的同步。

(3) 设计稳定-Niche双层集成架构，引入差异化预热机制（稳定先验权重 + Niche零启动），有效解决小样本条件下的权重冷启动问题。

(4) 建立严格的LODO-CV与嵌套盲测双重验证框架，通过内外层IC gap定量刻画调参偏乐观程度，为小样本金融预测研究提供了可参考的评估范式。

#pagebreak()

// ─────────────────────────────────────────────────────────────────────────────
// 二、模型介绍
// ─────────────────────────────────────────────────────────────────────────────
= 二、模型介绍

== 2.1　问题数学化描述

#hei[数据格式：]五只股票（A/B/C/D/E）各提供5天历史数据，每天27,601行Tick快照，每只股票33个原始字段（五档买卖报价量、成交信息、订单流指标）。

#hei[预测目标（标签）：]股票E的未来五分钟中间价收益率

$ y(t) = "Return5min"(t) = frac("MidPrice"(t+600) - "MidPrice"(t), "MidPrice"(t)) $

其中 $"MidPrice"(t) = (p_1^b(t) + p_1^a(t)) \/ 2$（最优买一、卖一价均值），600个Tick ≈ 5分钟。

#hei[在线预测约束：]预测函数 $hat(y)(t) = f_theta (bold(x)_1, bold(x)_2, ..., bold(x)_t)$ 在 $t$ 时刻只能使用 $t$ 及之前的已知信息，不引入任何未来数据（严格无前视偏差）。

#hei[评价指标：]
- 信息系数（IC）：$"IC"_d = "Pearson"(hat(bold(y))_d, bold(y)_d)$，逐天计算后取均值，反映预测值与真实值的线性相关程度 @ic_metric（第53页）。
- ICIR：$"ICIR" = overline("IC") \/ sigma("IC")$，即IC的均值除以标准差，衡量预测稳定性；ICIR越高表明IC跨日波动越小 @ic_metric（第55页）。

== 2.2　理论基础

=== 2.2.1　委托量失衡（OVI）的数学推导

委托量失衡（OVI）是本模型中单因子预测力最强的信号（均值IC≈0.26）。理论基础来自Kyle模型：当买方挂单量远大于卖方挂单量时，反映潜在买入需求超过卖出压力，预示短期价格上行。形式上，五档总委托量失衡定义为：

$ "OVI"(t) = frac(sum_(i=1)^5 V_i^b(t) - sum_(i=1)^5 V_i^a(t), sum_(i=1)^5 V_i^b(t) + sum_(i=1)^5 V_i^a(t) + epsilon) in [-1, 1] $

其中 $epsilon > 0$ 为防止除零的平滑系数，$V_i^b, V_i^a$ 分别为第 $i$ 档买单量和卖单量。

"OVI脉冲"（Pulse）信号通过对OVI做短长窗口SMA差分来提取近期动能偏离：

$ "OVI_pk"(t) = overline("OVI")_k(t) - overline("OVI")_600(t), quad k in {15, 30, 60} $

其中 $overline("OVI")_k(t) = frac(1, k) sum_(s=t-k+1)^t "OVI"(s)$ 为过去 $k$ 个Tick的简单移动平均。当 $k < 600$ 时，$"OVI_pk" > 0$ 表明近期委托压力高于日内均值，对应短期上涨预期，形成正向预测信号。

=== 2.2.2　成交量失衡（TI）因子

成交量失衡定义为：

$ "TI"(t) = frac(V_"buy"(t) - V_"sell"(t), V_"buy"(t) + V_"sell"(t) + epsilon) $

其中 $V_"buy", V_"sell"$ 分别为主动买入和主动卖出成交量（由逐笔成交数据的买卖方向判断）。TI直接对应Kyle模型中的净订单流 $u_t$，是价格冲击的成交层面体现。

=== 2.2.3　深层委托簿失衡（OBI）

根据Glosten-Milgrom模型，深层委托簿（2–5档）的不对称反映市场参与者对方向性风险的更深层判断：

$ "OBI"(t) = frac(sum_(i=2)^5 V_i^b(t) - sum_(i=2)^5 V_i^a(t), sum_(i=2)^5 V_i^b(t) + sum_(i=2)^5 V_i^a(t) + epsilon) $

相比OVI（1–5档合计），OBI着重捕捉"耐心资金"的方向偏好，提供与OVI部分正交的信息维度。

=== 2.2.4　AMH与多模型集成的必要性

自适应市场假说（AMH）@amh 预言：在不同市场状态（日期D1的高波动竞价、日期D4的低波动趋势行情等）下，不同微观结构信号的有效性呈现时变性。这意味着没有单一模型能在所有日期上保持最优表现，必须通过动态权重机制选择当日的"强信号模型"组合。实验数据印证了这一点：Day2（高波动）的最优子模型组合与Day4（低波动趋势）完全不同。

== 2.3　整体框架

如图1所示，整体框架分为离线训练和在线推理两阶段。

#figure(
  image("figures/fig6_architecture.png", width: 100%),
  caption: [系统整体架构图（离线训练 + 在线推理双阶段）],
) <fig6>

#hei[离线训练阶段]（`train_model.py`）：以五日全量数据为训练集，采用五折按日交叉验证评估。训练内容：(1) 计算59个特征并做z-score标准化；(2) 训练28个Ridge子模型，保存系数向量 $bold(w)_i$ 和偏置 $b_i$；(3) 训练集均值/标准差同步保存至 `models.pkl`。

#hei[在线推理阶段]（`MyModel.py`）：每收到一个新Tick，执行：(1) 增量更新SMA/EMA滑动窗口状态，计算59维特征向量 $bold(x)(t)$；(2) 对28个子模型线性推理 $hat(y)_i(t) = bold(w)_i^top bold(x)(t) + b_i$；(3) 滚动IC-Softmax动态集成得到最终预测 $hat(y)(t)$。

== 2.4　动态集成机制的数学推导

设第 $i$ 个子模型在滑动窗口 $[t - W, t - d)$ 内的滚动IC为：

$ "IC"_i(t) = "Pearson"\( {hat(y)_i(s)}_(s=t-W)^(t-d),\ {y(s)}_(s=t-W)^(t-d) \) $

其中 $W = 600$（约5分钟），$d = 600$（标签滞后，确保 $y(t-d) = "Return5min"(t-d)$ 在 $t$ 时刻已知）。

EWMA平滑抑制IC估计的短期噪声，半衰期约为 $T_(1/2) = -ln 2 / ln(1-beta) approx 99$ 步（$beta=0.007$）：

$ tilde("IC")_i(t) = (1-beta) dot tilde("IC")_i(t - Delta) + beta dot "IC"_i(t) $

其中 $Delta = 15$（每15个Tick更新一次）。集成权重通过带温度的Softmax归一化：

$ w_i(t) = frac(exp(tilde("IC")_i(t) dot tau), sum_(j=1)^N exp(tilde("IC")_j(t) dot tau)), quad tau = 17 $

温度参数 $tau$ 控制权重集中程度：$tau$ 越大，强IC模型获得越高权重。最终预测：

$ hat(y)(t) = sum_(i=1)^N w_i(t) dot hat(y)_i(t) $

子模型分为两类，预热期权重处理有所不同：
- #hei[稳定模型（MTC）：] 1个，在窗口积累不足时享有 $"stable_prior" = 2.0$ 倍先验权重，保证早盘预测稳健性。
- #hei[Niche模型：] 27个，预热期初始权重为0（零预热机制）；窗口积累充足后才按IC参与集成，防止噪声IC污染早期权重估计。

// ─────────────────────────────────────────────────────────────────────────────
// 三、测试流程
// ─────────────────────────────────────────────────────────────────────────────
= 三、测试流程

== 3.1　特征和标签选取

=== 3.1.1　标签选取

预测目标为股票E未来五分钟中间价收益率：

$ y(t) = frac("MidPrice"(t+600) - "MidPrice"(t), "MidPrice"(t)) $

$t+600$ 时刻的中间价在 $t$ 时刻是未知的，因此模型在 $t$ 时刻输出预测 $hat(y)(t)$，训练时以真实 $y(t)$ 计算损失。对标签不做任何平滑或变换，保持原始收益率形式以便IC评估。

=== 3.1.2　特征选取原则

特征选取遵循以下原则：(1) #hei[无前视偏差]——所有特征仅使用 $t$ 时刻及之前的数据；(2) #hei[全日IC方向一致]——候选特征须在全部5个测试日上IC符号一致（正向预测力稳定）；(3) #hei[正交互补]——新增特征与现有特征的相关系数须 $< 0.7$，提供独立信息维度；(4) #hei[极端值处理]——对收益率类特征实施剪裁防止异常值干扰。

=== 3.1.3　特征体系概览（59个特征，8大类）

#figure(
  image("figures/fig3_feature_categories.png", width: 80%),
  caption: [特征体系分类分布（共59个特征，8大类）],
) <fig3>

各类特征的数学定义与经济学含义如下：

(1) #hei[成交量失衡（TI）脉冲（8个）：] $"TI_p"k = overline("TI")_k - overline("TI")_600$（$k in {15,30,40,60}$），捕捉成交方向的短期偏离；另包含EMA版本 $"TI_ep"k$。

(2) #hei[委托量失衡（OVI）脉冲（5个）：] 如第2.2.1节定义，均值IC≈0.26，为最强单因子。

(3) #hei[委托/成交笔数失衡（4个）：] 笔数层面的方向信号 ONI（Order Number Imbalance）和 TNI（Trade Number Imbalance），与OVI量纲不同，提供正交信息。

(4) #hei[板块联动因子（7个）：] A/B/C/D四只股票的截面均值信号，包含板块OBI、板块TI脉冲、板块OVI脉冲等，捕捉系统性板块动量效应。

(5) #hei[日内时间特征（2个）：] `aft_13800`（下午标志，$t >= 13800$s）和 `aft_12000`，利用下午市场反转效应。

(6) #hei[滞后价格收益特征（12个）：] 包括E股和板块过去5分钟已实现收益（均值回复信号），以及E股30/60/120/300/600/900 Tick中间价收益率。

(7) #hei[委托簿深度与流量（7个）：] 深层委托簿失衡（OBI，2–5档）、买卖价差脉冲、大单成交失衡、日内累计成交流量失衡，捕捉"耐心资金"和机构行为。

(8) #hei[交互与高级信号（14个）：] 波动率条件化OVI（$"OVI" times |r|$）、截面异质OVI（"e_sect_lag_gap"，IC均值=0.203）、价格动量加速度（$r_15 - r_30$）、OVI平方项及多重两两交互（如 $"OVI" times "TBV"$），为线性Ridge模型提供非线性信息。

== 3.2　特征预处理

=== 3.2.1　在线增量计算

所有特征均通过增量式数据结构实时计算，时间复杂度为 $O(1)$ 每Tick：

- `_RunSMA(window)`：固定窗口滑动均值，使用双端队列（`deque`）维护窗口。
- `_RunEMA(span)`：指数移动平均，$"EMA"_t = (1-alpha) dot "EMA"_{t-1} + alpha dot x_t$，$alpha = 2 / ("span" + 1)$。

=== 3.2.2　冷启动处理

日初前 $k$ 个Tick（$k$ 为特征滞后长度）无历史数据，使用当日有效值的运行均值填充，而非用 $0$ 填充。用 $0$ 填充会产生"无信号"先验，在冷启动期制造可学习但不泛化的假信号；中性均值填充可避免模型学到虚假冷启动规律 @fillna。

=== 3.2.3　标准化

Ridge回归对特征量纲敏感。训练时，各子模型在训练集上做 $z$-score 标准化（$hat(x) = (x - mu) \/ sigma$）；在线推理时使用训练阶段保存的 $mu, sigma$ 做相同变换。均值和标准差随模型系数存入 `models.pkl`，保证训练-推理一致性。

=== 3.2.4　极端值裁剪

对收益率类特征实施截断防止异常值影响：

- 长窗口收益率（$>= 120$ Tick）：$x arrow.l "clip"(x, -0.1, 0.1)$
- 短窗口收益率（30/60 Tick）：$x arrow.l "clip"(x, -0.05, 0.05)$
- 交互特征（乘积项）：额外乘以归一化系数后裁剪至 $[-0.5, 0.5]$，防止乘积项方差过大。

== 3.3　训练集和交叉验证集的设置

=== 3.3.1　五折按日交叉验证（LODO-CV）

由于训练数据仅5天（Fold数等于日数），采用Leave-One-Day-Out交叉验证（LODO-CV）@lodo_cv：每折将一天作为测试集，其余四天作为训练集，共5折。该方案的优点：

- #hei[最大化训练数据利用率：] 每折使用4天训练，贴近最终全量训练条件。
- #hei[测试日相互独立：] 不同折的测试日无交叉，IC均值为5天的无偏估计。
- #hei[对齐竞赛评分机制：] 竞赛评分按天取IC均值，LODO-CV与之完全一致。

实现中以 `GroupKFold(n_splits=5)` 配合 `Day` 列为分组标识，确保同一天的数据不会同时出现在训练集和测试集。

=== 3.3.2　嵌套盲测验证

为定量评估调参过拟合程度，还实施了嵌套盲测（Nested Blind Test）：

- #hei[外层（盲测IC）：] 用其余4天训练，在保留天完整预测，等价于LODO-CV。验证代码层面无数据泄露。
- #hei[内层（调参IC）：] 在4天训练集内部运行4折CV，是研究者调参时实际参考的数字，包含对4天数据的过拟合。
- #hei[内-外gap：] $"gap" = "IC"_"inner" - "IC"_"outer"$，刻画调参偏乐观程度。Iter20最终模型的内外gap约为 $-0.019$（外层略高，属合理范围），证明调参过拟合可控。

== 3.4　模型训练与参数设定

=== 3.4.1　Ridge回归子模型的理论选择

每个子模型为带 $L_2$ 正则化的线性回归（Ridge Regression）@ridge：

$ min_(bold(w), b) ||bold(y) - bold(X)bold(w) - b||_2^2 + alpha ||bold(w)||_2^2 $

解析解为：$hat(bold(w)) = (bold(X)^top bold(X) + alpha bold(I))^(-1) bold(X)^top bold(y)$

Ridge相比OLS的优势在于：(1) 当特征高度共线（多个OVI/TI特征高度相关）时，OLS系数估计方差极大，Ridge通过 $alpha$ 控制方差-偏差权衡；(2) 相比LightGBM/神经网络，Ridge在样本量仅约 $n = 138000$ 行、特征维度 $p = 59$ 时泛化性更强，不易过拟合。

正则化系数 $alpha$ 按模型特性设定：多数模型 $alpha=150$（综合特征集），OVI纯净模型 $alpha=10$（低维，需弱正则），高维交互特征模型 $alpha=1000$（防过拟合）@ridge。

=== 3.4.2　28个子模型的设计

如表1所示，28个子模型覆盖了多种特征子集和时间段组合。1个稳定模型（MTC）负责全时段稳健预测，作为预热期基准；27个niche模型针对特定信号组合和时段，在各自擅长的市场条件下表现突出。

#figure(
  kind: table,
  caption: [子模型分类概览],
  table(
    columns: (auto, auto, auto, auto),
    align: (left, center, center, left),
    stroke: 0.5pt,
    [#hei[模型类别]], [#hei[数量]], [#hei[正则化 $alpha$]], [#hei[特征重点]],
    [稳定（Stable）], [1], [150], [TI纯净 + 时间特征（MTC）],
    [OVI纯净niche], [4], [10–150], [OVI脉冲系列为主要输入],
    [截面反转niche], [3], [150], [`sect_ret_lag` + `e_ret_lag` + `e_sect_lag_gap`],
    [非线性niche], [4], [150–1000], [`spread_wt_ovi`、`ovi_sq`、交互特征族],
    [深度/流量niche], [6], [150], [`obi_deep_p15`、`cum_flow_imb`、`book_pres_pulse`],
    [综合多因子niche], [10], [150–500], [多信号组合],
  )
) <tab1>

=== 3.4.3　迭代优化历程

如图2所示，模型经历了20轮迭代优化，IC从0.147稳步提升至0.3302：

#figure(
  image("figures/fig1_ic_evolution.png", width: 95%),
  caption: [模型IC迭代演进曲线（Iter0→Iter20）],
) <fig1>

主要里程碑包括：

- #hei[Iter0–2（基础建设）：] 建立严格LODO-CV评估体系，IC从0.147提升至0.224（+52%）。
- #hei[Iter3–8（集成引入）：] 设计动态集成机制，引入日内时间特征和滞后收益特征，IC提升至0.279。
- #hei[Iter9–12（突破0.30）：] 引入累计成交流量失衡和5个交互特征，配合IXN niche模型族，IC首次突破0.30，达到0.300。
- #hei[Iter13–19（精细优化）：] 逐步添加动量加速度、波动率条件化、截面异质OVI等高级信号，IC稳步提升至0.313，ICIR达7.82。
- #hei[Iter20（反过拟合剪枝）：] 采用LOO剪枝，将54个模型精简为28个。剪枝后IC跃升至0.330，ICIR从7.82飙升至12.59（+61%），证明合理的集成压缩可同时提升IC和稳定性。

=== 3.4.4　集成参数设定

#figure(
  kind: table,
  caption: [动态集成参数汇总],
  table(
    columns: (auto, auto, auto),
    align: (left, center, left),
    stroke: 0.5pt,
    [#hei[参数]], [#hei[值]], [#hei[含义]],
    [`ENSEMBLE_WINDOW`], [600], [滚动IC计算窗口（约5分钟）],
    [`ENSEMBLE_TEMP`], [17], [Softmax温度（权重集中程度）],
    [`ENSEMBLE_FLOOR`], [0], [权重下界（允许权重降至0）],
    [`RETURN_DELAY`], [600], [IC计算所需标签滞后（Tick）],
    [`ENSEMBLE_UPDATE_FREQ`], [15], [权重更新频率（每15 Tick）],
    [`ENSEMBLE_EWMA_BETA`], [0.007], [IC平滑系数（EWMA，半衰期约99 Tick）],
    [`STABLE_PRIOR`], [2.0], [稳定模型预热期先验权重倍数],
    [`NICHE_INIT_WEIGHT`], [0], [niche模型预热期初始权重],
  )
) <tab2>

== 3.5　测试结果展示与模型评价

=== 3.5.1　逐日IC结果

图3展示了最终模型（Iter20）的五折CV逐日IC：

#figure(
  image("figures/fig2_final_cv.png", width: 85%),
  caption: [Iter20最终模型五折CV逐日IC（均值=0.330，ICIR=12.59）],
) <fig2>

五天测试IC分别为：Day1=0.326、Day2=0.363、Day3=0.338、Day4=0.284、Day5=0.341，均值IC=0.3302，标准差=0.026，ICIR=12.59。Day4（低波动趋势日）IC最低（0.284），这与AMH的预测一致：微观结构信号在趋势性行情中预测力相对弱。五天IC标准差仅0.026，体现了极高的跨日稳定性。

=== 3.5.2　基线对比

图4展示了从Iter1（6特征线性基线）到Iter20（28模型动态集成）的逐日IC提升：

#figure(
  image("figures/fig4_iter_comparison.png", width: 90%),
  caption: [Iter1基线 vs Iter20最终模型逐日IC对比],
) <fig4>

相比基线，Iter20在每一天上均取得显著提升：Day1提升72.5%（0.189→0.326），Day3提升134.7%（0.144→0.338），Day4提升129.0%（0.124→0.284）——弱信号日（Day1/3/4）的改善尤为突出，体现了多因子集成对多样化市场状态的覆盖能力。

=== 3.5.3　动态集成增益验证

图5展示了嵌套盲测中动态集成相对稳定等权基准的增益：

#figure(
  image("figures/fig5_ensemble_gain.png", width: 95%),
  caption: [动态集成 vs 稳定等权基准的IC增益（嵌套盲测，Iter19机制相同）],
) <fig5>

动态集成在所有5天上均优于等权基准，平均增益+0.051，验证了滚动IC-Softmax机制的有效性 @softmax_ensemble。增益最大的是Day5（+0.104），该天niche模型的滚动IC高度分化，动态权重精准识别了强信号模型。

=== 3.5.4　综合模型评价

#figure(
  kind: table,
  caption: [最终模型（Iter20）综合评价指标],
  table(
    columns: (auto, auto, auto),
    align: (left, center, left),
    stroke: 0.5pt,
    [#hei[评价维度]], [#hei[指标值]], [#hei[说明]],
    [五折CV均值IC], [0.3302], [主要竞赛指标，out-of-sample估计],
    [五折CV IC标准差], [0.026], [跨日稳定性，越低越好],
    [ICIR（IC/Std）], [12.59], [风险调整后IC，历史最高],
    [Penalized（M$-$0.5S）], [0.317], [惩罚性指标，兼顾均值与方差],
    [内层4折IC], [0.311], [调参参考值（含轻微调参偏差）],
    [内-外gap], [$-$0.019], [负值=外层略高，调参过拟合可控],
    [集成增益（vs等权）], [+0.051], [动态集成相对等权基准提升],
    [模型复杂度], [28子模型，59特征], [相比Iter19（54×59）更简洁],
  )
) <tab3>

// ─────────────────────────────────────────────────────────────────────────────
// 四、创新点分析
// ─────────────────────────────────────────────────────────────────────────────
= 四、创新点分析

== 4.1　Leave-One-Model-Out（LOO）反过拟合剪枝算法

传统集成学习通常倾向于"越多越好"，但在小样本（5天数据）条件下，子模型数量过多会导致集成噪声放大。本文提出LOO剪枝算法，其核心思想是：直接度量每个子模型对集成性能的边际贡献，剔除"负贡献"模型。

设集成性能指标为 $"OM" = overline("IC") - 0.5 sigma("IC")$（均值-方差惩罚型），LOO增益定义为：

$ Delta"OM"_m = "OM"(text("全模型")) - "OM"(text("去除") m) $

若 $Delta"OM"_m < 0$（即去除模型 $m$ 后集成性能提升），则该模型对集成有净负贡献，应予剪枝。通过此分析，Iter19的54个模型被精简至28个，OM从0.283提升至0.317，ICIR从7.82飙升至12.59（+61%）。LOO剪枝实质上是一种贪心式逐步特征选择在模型集成层面的应用 @loo_prune。

== 4.2　稳定-Niche双层预热架构

本文首次将集成子模型显式分为"稳定模型"和"Niche模型"两类，并为二者设计差异化的预热机制。

在滚动窗口期不足（$t < W + d = 1200$ Tick，约前10分钟）时，各模型缺乏足够的IC历史：(1) 稳定模型（MTC）在此期间享有 $"stable_prior" = 2.0$ 倍先验权重，保证早盘预测稳健性；(2) Niche模型初始权重为0（零预热机制），只有当积累足够IC历史后才参与集成，防止噪声IC影响早期权重估计。

该设计解决了在线集成中"冷启动"与"全力预测"的矛盾，在Day1早盘（历史数据最少）上表现尤为关键。

== 4.3　截面板块异质信号（e_sect_lag_gap）

传统Tick预测仅利用单只股票的自身信号。本文引入7个板块截面因子。其中最具创新性的是"截面异质OVI"（"e_sect_lag_gap"）：

$ "e_sect_lag_gap"(t) = "OVI_p15"^E(t) - "Sect_OVI_p20"(t) $

即股票E自身的近期OVI相对板块均值的偏差。当 $"e_sect_lag_gap" > 0$ 时，E股的买方委托压力超过板块平均水平，代表E股特异性（idiosyncratic）看涨信号，经实验验证均值IC高达0.203，是最后几轮迭代中单次提升最大的新因子。

== 4.4　在线增量计算框架

本文所有59个特征均采用增量式数据结构实现 $O(1)$ 时间复杂度更新，支持真实在线推理（每收到一个Tick立即输出，无批处理延迟）。具体而言，SMA使用双端队列维护定长滑动窗口，EMA仅保存上一步状态 @ewma_forecast，累计流量信号在每日收盘后重置日内计数器。这与离线批量计算的传统做法形成鲜明对比，体现了面向实盘部署的工程创新。

// ─────────────────────────────────────────────────────────────────────────────
// 五、应用前景
// ─────────────────────────────────────────────────────────────────────────────
= 五、应用前景

== 5.1　量化交易中的Alpha信号

本模型最直接的应用是作为高频量化交易策略的Alpha信号生成器。预测值 $hat(y)(t)$ 可直接用于指导超短期仓位方向：$hat(y)(t) > theta$ 时建立多头头寸，$hat(y)(t) < -theta$ 时建立空头头寸（$theta$ 为阈值，可通过回测优化）。五折CV IC=0.330意味着在合理交易成本假设下仍有显著的信号强度，具备实际部署价值。

== 5.2　市场做市与流动性管理

做市商可利用本模型对短期价格走势的预测优化挂单策略：当模型预测价格将上涨时，适当减少卖方挂单（降低逆向选择风险），并加大买方挂单深度（提供更具竞争力的流动性）。这可降低做市商的库存风险，提升净做市收益。

== 5.3　多资产扩展与通用性

本文框架具有较强的通用性和可扩展性：

- #hei[多标的扩展：] 板块截面信号天然支持从单标的扩展到全板块多标的预测，特征生成代码对股票数量无硬性限制。
- #hei[跨资产类别：] 订单流失衡信号在股指期货、ETF等电子化市场同样有效，框架对市场类型无特定假设。
- #hei[在线学习扩展：] 当前动态集成权重是轻量级在线学习的体现；未来可引入在线Ridge回归对子模型系数本身进行实时更新（Online Learning），进一步提升对市场状态切换的适应速度。

== 5.4　风险管理与市场状态监控

28个子模型的实时权重分布本身是市场状态的量化刻画：当OVI纯净模型权重主导时，说明委托簿信号当日占优；当截面反转模型权重上升时，说明系统性板块效应增强；当所有niche模型权重均低时，提示市场进入低信号质量状态（应降低仓位）。这为风险管理提供了实时的市场状态指示器，具有超出单纯预测的附加价值。

// ─────────────────────────────────────────────────────────────────────────────
// 六、结论
// ─────────────────────────────────────────────────────────────────────────────
= 六、结论

本文以市场微观结构理论为数学原型，构建了一套端到端的多因子动态集成Tick级收益率预测系统，取得了以下主要成果：

(1) 基于Kyle（1985）连续拍卖模型和Glosten-Milgrom（1985）逆向选择模型，从理论上推导了OVI、TI、OBI等核心因子的数学定义，并引入自适应市场假说（AMH）为动态集成机制提供理论依据，确保了模型设计的数学严谨性和经济学一致性。

(2) 提出LOO反过拟合剪枝算法，在集成层面将子模型从54精简至28，IC从0.313提升至0.330，ICIR从7.82大幅提升至12.59（+61%），证明合理的集成压缩可同时提升预测精度和稳定性。

(3) 设计稳定-Niche双层预热架构和截面异质OVI信号（"e_sect_lag_gap"，IC=0.203），显著提升了集成在早盘冷启动期和跨日市场状态切换时的鲁棒性。

(4) 经严格的五折LODO-CV与嵌套盲测双重验证，最终模型（Iter20）实现五折CV均值IC=0.3302、ICIR=12.59，相对初始基线（IC=0.147）提升124.6%，内外层IC gap=−0.019，调参偏乐观程度在合理可控范围内。

#hei[局限与展望：] (1) 数据仅5天，样本量有限，统计置信度存在不确定性；(2) Day4（低波动趋势日）IC仍偏低（0.284），趋势行情下微观结构信号的预测力是未来改进的主要方向；(3) 未来可探索在线Ridge回归、强化学习权重优化等更高级的在线学习机制，以及引入更多截面股票进一步挖掘跨资产信息。

// ─────────────────────────────────────────────────────────────────────────────
// 参考文献
// ─────────────────────────────────────────────────────────────────────────────
= 参考文献

#bibliography("references.bib", style: "gb-7714-2015-numeric", title: none)
