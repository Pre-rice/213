// 排版设置
#set page(
  paper: "a4",
  margin: (top: 2.5cm, bottom: 2.5cm, left: 3.0cm, right: 2.5cm),
  numbering: "1",
)

// 正文：小四号宋体，单倍行距
#set text(
  font: ("SimSun"),
  size: 12pt,
)

#set par(
  first-line-indent: 2.0em,
  leading: 1.0em,
  spacing: 1.2em,
  justify: true,
)

// 黑体辅助命令
#let hei(content) = text(
  font: ("SimHei"),
  content,
)

// 一级标题：四号黑体，居中
#show heading.where(level: 1): it => {
  set align(center)
  set text(
    font: ("SimHei"),
    size: 14pt,
    weight: "bold",
  )
  v(1.0em)
  it.body
  v(0.5em)
}

// 二级标题：小四号黑体，左对齐
#show heading.where(level: 2): it => {
  set par(first-line-indent: 0em)
  set align(left)
  set text(
    font: ("SimHei"),
    size: 12pt,
    weight: "bold",
  )
  v(0.6em)
  it.body
  v(0.3em)
}

// 三级标题：小四号黑体，左对齐
#show heading.where(level: 3): it => {
  set par(first-line-indent: 0em)
  set align(left)
  set text(
    font: ("SimHei"),
    size: 12pt,
    weight: "bold",
  )
  v(0.4em)
  it.body
  v(0.2em)
}

//------------------------------

// 标题
#align(center)[
  #text(
    font: ("SimHei"),
    size: 16pt,
    weight: "bold",
  )[基于板块效应构建的多因子动态集成股票收益率预测模型]
]

#v(1.2em)

= 作 者

#align(center)[
  #text(
    weight: "bold",
  )[李智  复旦大学  相辉学堂\
  徐智培  复旦大学  生命科学学院\
  欧阳子乐  复旦大学  计算与智能创新学院]
]

#v(1.2em)

= 摘 要

我们针对股票E的未来五分钟收益率预测问题，根据市场微观结构理论与机器学习理论，构建了一个基于板块效应的多因子动态集成预测模型。研究使用同一板块五只股票（A/B/C/D/E）的五日历史Tick级数据，任务是在线逐Tick预测股票E的未来五分钟收益率（Return5min）。

在特征工程方面，我们从原始五只股票的33个字段出发，提取了59个多维度因子，涵盖成交量失衡脉冲、委托量失衡脉冲、深层委托簿压力、板块均值信号特征等。在模型方面，我们训练了28个岭回归子模型（包含1个稳定模型与27个特化模型），配合基于EWMA平滑滚动IC的Softmax动态集成算法，实现在线动态权重分配。

我们采取五折按日交叉验证的评估方法，以五日平均IC与ICIR为主要评估标准，在简单线性模型的基础上，经20轮迭代优化，最终模型五折均值IC=0.3302，ICIR=12.59，表明模型具备较强的预测能力与较高的稳定性。经过进一步的嵌套盲测验证与在线接口的实现，我们确认模型过拟合可控，且具备在线预测可行性。

#v(1.2em)

= 关键词

股票收益率预测；市场微观结构理论；岭回归；多模型动态集成；EWMA平滑滚动IC；Softmax加权；按日交叉验证；ICIR

#pagebreak()

//------------------------------

= 模型介绍

== 1.1　问题描述

#hei[原始数据：]五只股票（A/B/C/D/E）各提供5天历史数据，每天27601行Tick快照，每只股票33个原始字段，包含快照类、委托类、成交类三种数据类型。

#hei[预测目标：]股票E的未来五分钟中间价收益率

$ "Return5min"(t) = frac("MidPrice"(t+600) - "MidPrice"(t), "MidPrice"(t)) $

其中 $"MidPrice"(t) = ("AskPrice1"(t) + "BidPrice1"(t)) / 2$

#hei[评价指标：]
股票E的未来5分钟预测收益率，与真实收益率的N日平均IC值。

== 1.2　模型架构

本模型的整体架构分为特征工程、离线训练和在线推理三个阶段。

#hei[特征工程阶段]（`data_processor.py`）：以市场微观结构理论为基础，提出了多个特征并进行筛选，最终提取了59个多维度因子，涵盖成交量失衡脉冲、委托量失衡脉冲、深层委托簿压力、板块均值信号特征等，将处理后的数据统一保存为`train.csv`。

#hei[离线训练阶段] （`train_model.py`）：以`train.csv`为训练数据，采取五折按日交叉验证的评估方式，以五日平均IC与ICIR（信息比率，IC均值与IC标准差的比值）为主要标准进行模型迭代，最终训练了28个岭回归子模型，模型参数保存至 `models.pkl`。

#hei[在线推理阶段] （`MyModel.py`）：在线复现特征工程的计算，调用保存好的模型，并基于EWMA平滑滚动IC动态评估各个模型在当日历史的预测效果，以此进行多模型Softmax动态集成，加权后得到最终预测结果。

本模型以市场微观结构理论为严格的经济学基础构建特征工程体系，采用岭回归进行模型训练以应对高维多重共线性等问题，并采取了较为创新的稳健模型与特化模型相结合的动态集成策略。下面将详细阐述每个环节的设计思路与理论依据。

== 1.3  特征选取的理论依据(难搞)
本研究的特征工程以 *市场微观结构理论*（Market Microstructure Theory）为经济学支撑。以下按特征类别，阐述主要信号的数学定义与经济学依据。

=== 1.3.1  订单流失衡与价格发现（Kyle + OFI）
Kyle (1985) @kyle1985 的连续拍卖模型指出，做市商在均衡中依据观测到的净交易需求（net order flow）$u_t$ 调整报价，典型线性形式为
$ p_t = p_(t-1) + lambda u_t,  lambda > 0. $
因此短期价格变动满足
$ Delta p_t = p_t - p_(t-1) = lambda u_t, $
即净订单流是短期价格变动的重要因果前驱。

在高频数据中，$u_t$ 不可直接观测，本文以“方向性失衡”构造可计算代理，并区分两层订单流。

*成交流（executed trade flow）*：令 tick 内主动买/卖成交量为 $V_t^("buy"), V_t^("sell")$，定义成交量失衡（TI）为
$ "TI"_t = (V_t^("buy") - V_t^("sell")) / (V_t^("buy") + V_t^("sell") + epsilon). $
（成交笔数失衡 TNI 可同理定义。）

*委托流（submission flow）*：令 tick 内新增买/卖委托量为 $O_t^("buy"), O_t^("sell")$，定义委托量失衡（OVI）为
$ "OVI"_t = (O_t^("buy") - O_t^("sell")) / (O_t^("buy") + O_t^("sell") + epsilon). $
（委托笔数失衡 ONI 可同理定义。）

为强调“近期冲击相对长期基线的偏离”，对任意序列 $x_t$（如 TI/OVI）定义滑动均值
$ overline(x)_k(t) = (1/k) sum_(i=0)^(k-1) x_(t-i), $
并构造冲变换
$ x_(p,k)(t) = overline(x)_k(t) - overline(x)_600(t). $
若 $x_t = mu_t + delta_t$ 且 $mu_t$ 在 600 tick 尺度上变化缓慢，则 $overline(x)_600(t) approx mu_t$，从而 $x_(p,k)$ 近似提取短期冲击分量 $delta_t$。Cont et al. (2014) @ofi 的实证结果进一步确认订单流失衡与中间价变动之间的显著统计关系。

=== 1.3.2  流动性、逆向选择与订单簿存量（价差 / 深度 / deep OBI）
仅用订单流解释价格变化隐含 $lambda$ 恒定，但微观结构强调冲击系数随流动性与信息不对称变化。Glosten--Milgrom (1985) @glosten1985 指出，逆向选择风险上升时做市商会提高补偿，表现为价差扩大、可得深度下降；因此可写为状态依赖冲击
$ Delta p_t approx lambda_t u_t,  lambda_t = lambda("liquidity"_t, "asymmetry"_t). $

本文加入订单簿存量与流动性成本信号刻画 $lambda_t$。

*相对价差与其脉冲*：
$ "spread"_t = ("AskPrice"_(t,1) - "BidPrice"_(t,1)) / "MidPrice"_t, $
$ "spread_pulse"_t = "spread"_t - overline("spread")_600(t). $

*深层委托簿失衡（2--5档）*：设第 $l$ 档买/卖挂单量为 $"BidVol"_(t,l), "AskVol"_(t,l)$，定义
$ "OBI"_t^("deep") =
  (sum_(l=2)^5 "BidVol"_(t,l) - sum_(l=2)^5 "AskVol"_(t,l))
/ (sum_(l=2)^5 "BidVol"_(t,l) + sum_(l=2)^5 "AskVol"_(t,l) + epsilon). $

价差与深度反映流动性成本与信息不对称状态；deep OBI 则反映更耐心的存量供需倾斜，与新增委托流（OVI）互补，从而共同决定订单流冲击是否更可能延续或回撤。

=== 1.3.3  板块联动：共同冲击与特异性偏离（截面因子）
板块内股票共享行业信息与资金流，短期存在共同因子。设板块集合为 $cal(S) = {A, B, C, D}$，对任意信号 $X_(s,t)$ 定义截面均值：
$ "SectX"_t = (1/|cal(S)|) sum_(s in cal(S)) X_(s,t). $
并可同样构造其脉冲 $"SectX"_(p,k)$ 刻画板块共同冲击的短期增强或衰减。

为提取 E 股相对板块的异质性压力，定义相对偏离：
$ "RelX"_t = X_(E,t) - "SectX"_t. $
该“共同--特异”分解使模型既利用板块联动提高信噪比，又捕捉个股层面的定向资金与错位信号。Lo (2004) @amh 的自适应市场假说强调策略有效性随市场状态时变，为后续动态加权机制提供一般性动机。

=== 1.3.4  日内状态与价格动力学（非平稳性 + 动量/回归）
高频数据具有显著日内季节性（波动、价差、成交强度随时间系统变化）。本文以 tick_index 阈值构造日内 dummy（如 `aft_12000`、`aft_13800`）作为状态变量，使订单流与流动性信号在不同时段呈现不同边际效应。

同时引入多尺度历史收益刻画短期动量与均值回复。以中间价为例：
$ r_t^(k) = ("MidPrice"_t - "MidPrice"_(t-k)) / "MidPrice"_(t-k). $
并用差分刻画趋势增强/衰减（加速度）：
$ "ret_accel"_t = r_t^("k1") - r_t^("k2"),  "k1" < "k2". $

== 1.4　岭回归模型的数学原理与选择依据

=== 1.4.1　数学原理

岭回归通过在线性回归的损失函数中添加一个$L_2$正则化项来限制模型的复杂度，从而避免模型过度拟合训练数据。其优化目标为：

$ min_(bold(w), b) ||bold(y) - bold(X)bold(w) - b||_2^2 + alpha ||bold(w)||_2^2 $

其中$bold(X) in RR^(n times p)$为特征矩阵，$bold(y) in RR^n$为标签向量，$bold(w) in RR^p$为特征权重，$alpha > 0$为正则化强度超参数。

$L_2$正则化项通过对权重的平方和进行惩罚，让模型更倾向于减小权重的值，这可以防止模型过于依赖某些特征，从而更好地泛化到新数据。

此外，$L_2$正则化虽然鼓励权重向零收敛，但不会像$L_1$正则化那样将权重变成零。因此，$L_2$正则化通常只会导致较小但非零的权重值，从而使模型更加平滑和稳定。

=== 1.4.2　岭回归在本任务中的优势

#hei[应对多重共线性。]本模型的59个特征中，多类OVI脉冲彼此高度相关，TI脉冲族同理。存在多重共线性时，最小二乘法的系数极不稳定，微小的数据扰动会导致系数剧烈震荡。而岭回归通过$L_2$正则化显式压缩相关特征的系数幅度，在保留全部特征信息的同时大幅降低估计方差，获得更稳定的泛化性能。

#hei[在小样本下的强泛化性。]本模型的训练集规模约110000行（4天训练集）、特征为59个，从数据量看并不小，但有效独立样本数远低于行数（相邻Tick自相关性很强）。在有效样本受限的条件下，参数数量较少的正则化线性模型的泛化能力一般会优于高容量非线性模型（如LightGBM）。

#hei[相比Lasso回归的连续可微优势。]正如1.4.1节所述，Lasso回归（$L_1$正则化）容易将部分相关特征的系数直接归零，可能丢失组合预测力。而岭回归对所有特征施加连续可微的二次惩罚，保留全部59个因子的联合贡献，能够兼顾完整性与稳定性。

== 1.5　动态集成机制的提出依据与数学推导

=== 1.5.1　提出依据

由于预测目标Return5min的计算公式已知，在线预测时我们可以实时计算出五分钟前的Return5min指标。我们决定利用这一点，在线评估各个模型在当日历史的预测效果，并据此动态调整各模型的权重。

支持该做法的另一个依据是，我们发现单一模型在不同测试日或同日的不同时间段内的表现存在较大波动，且各个模型的优势区间存在显著差异，这表明不同模型在不同市场状态下具备互补的预测能力，多模型集成能够提升预测的稳定性。而动态集成相比静态加权，能够根据市场状态的变化灵活调整权重，充分利用各模型的优势，从而提升整体预测性能。

动态集成虽然能够快速适应市场变化，但也存在过度反应短期噪声的风险。为了减少这类不稳定风险，我们使用1个稳定模型作为基准，在窗口积累不足时享有2倍先验权重，确保集成的稳健性；27个特化模型则采用零预热机制，初始权重为0，只有在积累足够IC历史后才参与集成，防止噪声IC污染早期权重估计。

=== 1.5.2　数学推导

具体实现上，我们采取了基于EWMA平滑滚动IC的Softmax动态集成机制。

定义第 $i$ 个子模型在滑动窗口 $[t - d - W, t - d)$ 内的滚动IC为：

$ "IC"_i(t) = "Pearson"\( {hat(y)_i(s)}_(s=t-d-W)^(t-d), {y(s)}_(s=t-d-W)^(t-d) \) $

其中 $W = 600 "Tick"$（5分钟窗口长度），$d = 600 "Tick"$（5分钟时间滞后，确保标签 $y(t-d)$ 在 $t$ 时刻已知）。

该值衡量了模型在近期窗口内的预测效果，但是受短期噪声的影响较大。因此，我们没有直接用 $"IC"_i(t)$ 来做权重，而是采取了指数加权移动平均（EWMA）的方法，对历史数据赋予指数衰减的权重，从而得到更稳定的“模型得分” $tilde("IC")_i(t)$：

$ tilde("IC")_i(t) = (1-beta) dot tilde("IC")_i(t - Delta) + beta dot "IC"_i(t) $

其中 $beta = 0.007$（半衰期约99 Tick）控制了平滑程度，$Delta = 15$ 是权重更新频率（每15 Tick更新一次）。

最后，我们使用归一化指数函数（Softmax函数），把EWMA平滑后的滚动IC“得分”变成非负且和为1的模型权重：

$ w_i(t) = frac(exp(tilde("IC")_i(t) dot tau), sum_(j=1)^N exp(tilde("IC")_j(t) dot tau)) $

其中温度参数 $tau = 17$ 控制了权重的集中程度。

最终输出的预测结果即各模型的预测值根据动态权重的加权平均：

$ hat(y)(t) = sum_(i=1)^N w_i(t) dot hat(y)_i(t) $

//------------------------------

= 测试流程

== 2.1　特征和标签选取

=== 2.1.1　标签选取

模型的预测目标为股票E的未来五分钟收益率。我们在训练时直接把它作为唯一标签，保持原始未处理的形式以便进行IC评估。

=== 2.1.2　特征选取与筛选

我们基于1.3节所述的理论基础提出了多个方位的特征，并采用以下两个主要标准进行筛选，确保特征的有效性：

(1) #hei[特征与标签的IC]——计算候选特征与标签的五日IC，以评估特征与标签的相关性。其中稳健模型的特征须在全部测试日上具备同号的高IC，特化模型的部分特征则可以只在部分测试日上具备高IC。我们通过这种筛选方式，来保证选取的特征对于标签预测具备足够的有效性，避免引入噪声特征。

(2) #hei[正交互补]——新增特征与现有特征的IC不宜过高，以防止引入冗余特征。

=== 2.1.3　特征体系概览（待改写）

经过上述原则的筛选，我们最终构建了一个包含59个特征的多维度特征体系，涵盖了8大类，分别捕捉不同层次和维度的市场行为信号，为模型提供丰富且有效的预测信息。

#figure(
  image("figures/fig3_feature_categories.png", width: 80%)
)

各类特征的数学定义与经济学含义如下：

(1) #hei[成交量失衡（TI）脉冲（8个）：] $"TI_p"k = overline("TI")_k - overline("TI")_600$（$k in {15,30,40,60}$），捕捉成交方向的短期偏离；另包含EMA版本 $"TI_ep"k$。

(2) #hei[委托量失衡（OVI）脉冲（5个）：] 如第1.4.1节定义，均值IC≈0.26，为最强单因子。

(3) #hei[委托/成交笔数失衡（4个）：] 笔数层面的方向信号ONI（Order Number Imbalance）和TNI（Trade Number Imbalance），与OVI量纲不同，提供正交信息。

(4) #hei[板块联动因子（7个）：] A/B/C/D四只股票的截面均值信号，包含板块OBI、板块TI脉冲、板块OVI脉冲等，捕捉系统性板块动量效应。

(5) #hei[日内时间特征（2个）：] `aft_13800`（下午标志，$t >= 13800$s）和 `aft_12000`，利用下午市场反转效应。

(6) #hei[滞后价格收益特征（12个）：] 包括E股和板块过去5分钟已实现收益（均值回复信号），以及E股30/60/120/300/600/900 Tick中间价收益率。

(7) #hei[委托簿深度与流量（7个）：] 深层委托簿失衡（OBI，2–5档）、买卖价差脉冲、大单成交失衡、日内累计成交流量失衡，捕捉"耐心资金"和机构行为。

(8) #hei[交互与高级信号（14个）：] 波动率条件化OVI（$"OVI" times |r|$）、截面异质OVI（e_sect_lag_gap，IC均值=0.203）、价格动量加速度（$r_15 - r_30$）、OVI平方项及多重两两交互（如 $"OVI" times "TBV"$），为线性Ridge模型提供非线性信息。

== 2.2　特征预处理

=== 2.2.1　特征标准化

岭回归对特征量纲较为敏感，因此我们首先对特征在训练集上进行Z-score标准化（$hat(x) = (x - mu) \/ sigma$）；在线推理时使用训练阶段保存的 $mu, sigma$ 做相同变换，均值和标准差随模型系数存入 `models.pkl`。

=== 2.2.2　极端值裁剪

为防止少量极端值影响模型系数，我们对部分特征实施了极端值裁剪：

- 长窗口收益率（$>= 120$ Tick）：裁剪至 $[-0.1, 0.1]$
- 短窗口收益率（30/60 Tick）：裁剪至 $[-0.05, 0.05]$
- 交互特征（乘积项）：乘以归一化系数后裁剪至 $[-0.5, 0.5]$，防止乘积项方差过大。

=== 2.2.3　冷启动处理

日初的一段时间缺少历史数据，需要对部分特征进行冷启动处理。对于历史窗口类特征，在样本数不足时使用已有样本的均值代替，而非用0填充，这可以避免模型学到虚假的冷启动规律；对于滞后标签类特征，在日初无法计算时直接用0填充。

== 2.3　训练集和交叉验证集的设置

=== 2.3.1　五折按日交叉验证

我们在模型训练中采用五折按日交叉验证的方式，每折将一天作为测试集，其余四天作为训练集，重复五次。相比于固定训练集与验证集，该方法能够在避免数据泄漏的同时最大化利用训练数据，通过多次重复得到更加可信的结果，同时ICIR还能够有效评估模型的预测稳定性。

由于在线预测时的测试日互相独立，我们在训练时也没有刻意保持训练集的连续性，这样可以更好地模拟在线环境中的数据分布。此外，相比滚动时序交叉验证，按日交叉验证能够确保每折测试都具备足够的样本量，从而得到更有效的IC评估结果。

=== 2.3.2　嵌套盲测验证

五折按日交叉验证的方式存在过拟合五日数据的风险。为定量评估调参过拟合程度，我们还实施了嵌套盲测：
- #hei[内层（调参IC）：] 在4天训练集内部运行四折交叉验证，并以平均IC作为调参依据，包含对4天数据的过拟合。
- #hei[外层（盲测IC）：] 在保留天进行盲测，模拟在线预测，评估模型在未知日的表现。
- #hei[内外差值：] 重复上述过程五次后，对比内外层平均IC，差值约为$-0.019$（外层略高），证明调参过拟合程度在可控的范围内。

== 2.4　模型训练和参数设定

=== 2.4.1　岭回归模型

岭回归的数学原理与选择依据已在第1.3节详细阐述。实际模型训练中，我们也测试了Lasso回归、弹性网络、LightGBM等模型类别，但效果都不如岭回归，因此我们最终选择了岭回归作为模型的基础算法。

=== 2.4.2　子模型设计概览

我们最终训练了28个岭回归子模型，覆盖多种特征子集和时间段组合。1个稳定模型负责全时段稳健预测，作为预热期基准；27个特化模型则针对特定信号组合和时段，在各自擅长的市场条件下表现突出。子模型分类概览如下：

#figure(
  kind: table,
  table(
  columns: (auto, auto, auto, 1fr),
  align: (left, center, center, left),
  stroke: 0.5pt,

  [#hei[模型类别]], [#hei[数量]], [#hei[$alpha$]], [#hei[特征重点]],

  [稳定模型], [1], [200],
  [成交方向脉冲 + 日内时段特征],

  [委托方向特化], [1], [20],
  [委托买卖压力脉冲 + 委托笔数失衡],

  [深度/流量特化], [3], [20],
  [日内累计成交净流 + 深层委托簿不平衡],

  [截面板块特化], [4], [15--20],
  [个股相对板块偏离 + 板块/个股滞后收益差],

  [非线性/交互特化], [6], [15--20],
  [价差条件化委托压力 + 委托压力非线性放大 + 多信号交互确认],

  [综合多因子特化], [13], [15--20],
  [订单流 + 价格动量/回归 + 流动性与状态条件化的组合],
)
)

=== 2.4.3　模型选取与调参方法

具体的子模型选取方式上，我们以单模型五折交叉验证的IC为主要评估标准，筛选出表现较好的模型类别和特征组合。此外，在部分测试日或特定时段内表现较好的模型也被保留，以利用其在特定市场状态下的优势。

为避免模型冗余与过拟合，我们采取了Leave-One-Model-Out（LOO）的反过拟合剪枝方法，逐一移除各个子模型并观察五折交叉验证平均IC的变化，以评估每个子模型对集成性能的贡献。最终，我们从54个子模型中精简出28个，进一步提升了集成的IC和ICIR。

调参方式上，我们在选取的子模型上进行了细致的网格搜索调参，以五折交叉验证的平均IC作为调参依据，调整的参数包括岭回归的正则化强度$alpha$、动态集成机制中的EWMA平滑参数$beta$和Softmax温度参数$tau$等。

=== 2.4.4　迭代历程总览

如下图所示，模型经历20轮迭代优化，IC从基线的0.147稳步提升至0.3302：

#figure(
  image("figures/fig1_ic_evolution.png", width: 95%)
) 

主要里程碑：
- Iter0–2依据市场微观结构理论引入了多个特征，IC从0.147提升至0.224。
- Iter3–4引入动态集成机制与日内时间特征，IC提升至0.279。
- Iter9–12引入累计流量失衡和交互特征，IC突破0.30的目标。
- Iter13–19加入动量加速度、波动率条件化、截面异质OVI等高级信号，IC稳步提升至0.313，ICIR达7.82。
- Iter20采用LOO反过拟合剪枝，54个模型精简为28个，IC提升至0.330，ICIR从7.82跃升至12.59（+61%），模型稳定性大幅提升。

== 2.5　测试结果展示和模型评价

=== 2.5.1　逐日IC结果

下图展示了最终模型（Iter20）的五折交叉验证逐日IC：

#figure(
  image("figures/fig2_final_cv.png", width: 85%)
) 

五日均值IC达到0.3302，标准差仅0.026，ICIR=12.59，体现了较强的预测能力与较高的跨日稳定性。

=== 2.5.2　基线对比

下图展示了从Iter1（6特征线性基线）到Iter20（28模型动态集成）的逐日IC提升：

#figure(
  image("figures/fig4_iter_comparison.png", width: 90%)
) 

相比基线，Iter20在每一天上均取得显著提升，弱信号日（Day1/3/4）的改善尤为突出，体现了多因子集成对多样化市场状态的覆盖能力。

=== 2.5.3　动态集成增益验证

下图展示了嵌套盲测中动态集成相对稳定等权基准的增益：

#figure(
  image("figures/fig5_ensemble_gain.png", width: 95%)
) 

动态集成在5天上均优于等权基准，平均增益+0.051，验证了滚动IC-Softmax机制的有效性 @softmax_ensemble。增益最大的是Day5（+0.104），该日特化模型的滚动IC高度分化，动态权重精准识别了强信号模型。

=== 2.5.4　模型综合评价

#figure(
  kind: table,
  table(
    columns: (auto, auto, auto),
    align: (left, center, left),
    stroke: 0.5pt,
    [#hei[评价维度]], [#hei[指标值]], [#hei[说明]],
    [五折CV均值IC], [0.3302], [主要评估指标],
    [五折CV IC标准差], [0.026], [体现跨日稳定性],
    [ICIR（IC均值/标准差）], [12.59], [综合评价模型预测能力与稳定性],
    [内层4折IC], [0.311], [调参参考值（含轻微调参偏差）],
    [内外差值], [$-$0.019], [外层略高，调参过拟合可控],
    [集成增益], [+0.051], [动态集成相对等权基准提升],
    [模型复杂度], [28子模型，59特征], [相比Iter19（54×59）更简洁],
  )
)

总结：本模型基于市场微观结构理论构建多维度特征体系，结合岭回归的稳定性与动态集成的适应性，经过五折交叉验证与嵌套盲测验证等测试，被证实在股票E未来五分钟收益率预测任务中具备较强的综合预测能力。

//------------------------------

= 参考文献(需完全重写)

#bibliography("references.bib", style: "gb-7714-2015-numeric", title: none)
