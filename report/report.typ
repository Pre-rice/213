// 排版设置
#set page(
  paper: "a4",
  margin: (top: 2.5cm, bottom: 2.5cm, left: 3.0cm, right: 2.5cm),
  numbering: "1",
)

// 正文：小四号宋体，单倍行距
#set text(
  font: ("SimSun"),
  size: 12pt,
)

#set par(
  first-line-indent: 2.0em,
  leading: 1.0em,
  spacing: 1.2em,
  justify: true,
)

// 黑体辅助命令
#let hei(content) = text(
  font: ("SimHei"),
  content,
)

// 一级标题：四号黑体，居中
#show heading.where(level: 1): it => {
  set align(center)
  set text(
    font: ("SimHei"),
    size: 14pt,
    weight: "bold",
  )
  v(1.0em)
  it.body
  v(0.5em)
}

// 二级标题：小四号黑体，左对齐
#show heading.where(level: 2): it => {
  set par(first-line-indent: 0em)
  set align(left)
  set text(
    font: ("SimHei"),
    size: 12pt,
    weight: "bold",
  )
  v(0.6em)
  it.body
  v(0.3em)
}

// 三级标题：小四号黑体，左对齐
#show heading.where(level: 3): it => {
  set par(first-line-indent: 0em)
  set align(left)
  set text(
    font: ("SimHei"),
    size: 12pt,
    weight: "bold",
  )
  v(0.4em)
  it.body
  v(0.2em)
}

//------------------------------

// 标题
#align(center)[
  #text(
    font: ("SimHei"),
    size: 16pt,
    weight: "bold",
  )[基于板块效应构建的多因子动态集成股票未来收益率预测模型]
]

#v(1.2em)

= 作 者

#align(center)[
  #text(
    weight: "bold",
  )[李智  复旦大学  相辉学堂\
  徐智培  复旦大学  生命科学学院\
  欧阳子乐  复旦大学  计算与智能创新学院]
]

#v(1.2em)

= 摘 要

我们针对股票E的未来五分钟收益率预测问题，以市场微观结构理论为数学原型，构建了一套基于板块效应的多因子动态集成预测模型。研究使用同一板块五只股票（A/B/C/D/E）的五日历史Tick数据，任务是在线逐Tick预测股票E的未来五分钟收益率（Return5min）。

在特征工程方面，我们从原始五只股票的33个字段出发，提取了59个多维度因子，涵盖成交量失衡脉冲、委托量失衡脉冲、深层委托簿压力、板块均值信号特征等。在模型方面，采用28个岭回归子模型（包含1个稳定模型与27个特化模型），配合基于EWMA平滑滚动IC的Softmax动态集成算法，实现在线动态权重分配。

我们采取五折按日交叉验证的评估方法，以五日平均IC与ICIR为主要评估标准，在简单线性模型的基础上，经20轮迭代优化，最终模型五折均值IC=0.3302，ICIR=12.59，表明模型具备不错的预测能力与较高的稳定性。经过进一步的嵌套盲测验证与在线接口的实现，我们确认模型无数据泄露，具备在线预测可行性。

#v(1.2em)

= 关键词

股票收益率预测；岭回归；多模型动态集成；滚动IC加权；按日交叉验证；ICIR

#pagebreak()

//------------------------------

= 模型介绍

== 1.1　问题描述

#hei[原始数据：]五只股票（A/B/C/D/E）各提供5天历史数据，每天27,601行Tick快照，每只股票33个原始字段，包含快照类、委托类、成交类三种数据类型。

#hei[预测目标（标签）：]股票E的未来五分钟中间价收益率

$ "Return5min"(t) = frac("MidPrice"(t+600) - "MidPrice"(t), "MidPrice"(t)) $

其中 $"MidPrice"(t) = ("AskPrice1"(t) + "BidPrice1"(t)) / 2$

#hei[评价指标：]
股票E的未来5分钟预测收益率，与真实收益率的N日平均IC值。

== 1.2　模型架构

本模型的整体架构分为特征工程、离线训练和在线推理三个阶段。

#hei[特征工程阶段]（`data_processor.py`）：以微观经济学理论为基础，以特征与标签的IC为筛选标准，提取了59个多维度因子，涵盖成交量失衡脉冲、委托量失衡脉冲、深层委托簿压力、板块均值信号特征等，将处理后的数据统一保存为`train.csv`。

#hei[离线训练阶段] （`train_model.py`）：以`train.csv`训练集，采用五折按日交叉验证评估的方式进行迭代，最终训练28个岭回归子模型，模型保存至 `models.pkl`。

#hei[在线推理阶段] （`MyModel.py`）：在线复现特征工程的计算，并调用保存好的模型，同时动态评估各个模型在当日历史的预测效果，采取滚动IC-Softmax动态集成得到最终预测结果。

本模型以市场微观结构理论为严格的经济学基础构建特征工程体系，采用岭回归进行模型训练以应对高维多重共线性问题，并采取了较为创新的稳健模型与特化模型相结合的动态集成策略。下面将详细阐述每个环节的设计思路与理论依据。

== 1.3　特征选取的理论依据（需完全重写）

本研究的特征工程以#hei[市场微观结构理论]（Market Microstructure Theory）@microstructure 为经济学支撑。以下按特征类别，阐述各类信号的数学定义与经济学依据。

=== 1.3.1 订单流失衡信号

Kyle（1985）@kyle1985 连续拍卖模型证明，在均衡状态下，做市商根据观察到的净订单流$u_t$（知情交易者与噪音交易者的合并买卖需求）线性调整报价，净订单流是价格走势的因果前驱。这一理论直接为本文最强单因子提供经济学基础：委托量失衡（OVI，均值IC≈0.26）刻画当前委托簿上买卖方向的压力不对称，成交量失衡（TI）则反映已成交订单的方向性，均是净订单流$u_t$在委托与成交层面的实测代理变量。Cont等（2014）@ofi 通过实盘数据实证确认了订单流失衡与中间价变动的统计显著线性关系。"脉冲"处理（短窗口SMA减长窗口SMA）进一步提取近期订单流偏离日内均值的动能成分，过滤低频漂移噪声，保留具有预测价值的高频方向冲量。

=== 委托簿深度与逆向选择：耐心资金的方向信号（OBI / 大单 / 价差类）

Glosten-Milgrom（1985）@glosten1985 逆向选择模型指出：做市商通过观察委托簿中不同档位的挂单结构来对冲信息不对称风险——知情交易者倾向于将意向隐藏在深层委托队列中，因此买卖深层挂单量的不对称（OBI，2–5档失衡）是信息优势资金方向偏好的前瞻信号。相比表层OVI（1–5档合计），OBI着重刻画"耐心资金"的方向判断，与OVI构成部分正交的信息维度（实验中两者相关系数小于0.5）。大单成交失衡（lot_imb）则通过筛选大手笔订单进一步识别知情交易者行为，价差脉冲（e_spread_pulse）反映流动性成本与逆向选择风险的实时变化，共同构成委托簿深度类特征族。

=== 截面板块联动：信息溢出与特异性偏离（板块因子）

在A股市场中，同一板块内多只股票共享宏观信息冲击，信息优势资金会同步布局多只相关股票。这种信息溢出效应使得E股的价格运动可被同板块股票（A/B/C/D）的订单流动态所预测。板块截面均值信号（Sect\_OVI\_p20、Sect\_TI\_p40等）捕捉系统性板块动量，而截面异质OVI（e\_sect\_lag\_gap$= "OVI_p15"^E - "Sect_OVI_p20"$，均值IC=0.203）则提取E股相对板块的特异性偏离——若E股近期买压显著强于板块均值，意味着E股有特异性正向信息冲击，是单次迭代提升最大的新因子之一。自适应市场假说（Lo，2004）@amh 进一步指出，这类截面信号的有效性会随市场状态动态变化，这正是引入动态集成权重机制的核心动机。

=== 日内时间效应与价格惯性：行为周期与均值回复（时间 / 滞后收益类）

大量实证研究证实A股市场存在显著日内周期性效应：午后（13:00以后）市场参与者结构变化，散户占比上升，推动特定时段的价格动量与反转规律。本文引入`aft_13800`和`aft_12000`二值特征捕捉这一午后时段效应（下午IC相比上午平均高0.03~0.05），其经济学基础是日内流动性周期与投资者行为的结构性异质性。滞后价格收益特征（e\_ret\_lag、past\_ret系列）则源于订单流模型中的"价格惯性与均值回复"机制——知情交易者的持续买压会在短时间内形成价格动量，随后被噪音交易者的反向流动所修正；同时板块截面收益（csm\_ret\_120）通过排除系统性因子，提取E股的特异性价格偏离。

=== 1.3.2　核心特征的数学推导

=== 委托量失衡（OVI）

委托量失衡（OVI）是本模型中单因子预测力最强的信号（均值IC≈0.26）。五档总委托量失衡定义为：

$ "OVI"(t) = frac(sum_(i=1)^5 V_i^b(t) - sum_(i=1)^5 V_i^a(t), sum_(i=1)^5 V_i^b(t) + sum_(i=1)^5 V_i^a(t) + epsilon) in [-1, 1] $

其中 $epsilon > 0$ 为防止除零的平滑系数，$V_i^b, V_i^a$ 分别为第 $i$ 档买单量和卖单量。

"OVI脉冲"（Pulse）信号通过对OVI做短长窗口SMA差分来提取近期动能偏离：

$ "OVI_pk"(t) = overline("OVI")_k(t) - overline("OVI")_600(t), quad k in {15, 30, 60} $

其中 $overline("OVI")_k(t) = frac(1, k) sum_(s=t-k+1)^t "OVI"(s)$ 为过去 $k$ 个Tick的简单移动平均。当 $k < 600$ 时，$"OVI_pk" > 0$ 表明近期委托压力高于日内均值，形成正向预测信号。

=== 成交量失衡（TI）

成交量失衡定义为：

$ "TI"(t) = frac(V_"buy"(t) - V_"sell"(t), V_"buy"(t) + V_"sell"(t) + epsilon) $

其中 $V_"buy", V_"sell"$ 分别为主动买入和主动卖出成交量（由逐笔成交数据买卖方向判断）。TI直接对应Kyle模型中的净订单流 $u_t$，是价格冲击的成交层面体现。

=== 深层委托簿失衡（OBI）

根据Glosten-Milgrom模型，深层委托簿（2–5档）的不对称反映市场参与者对方向性风险的更深层判断：

$ "OBI"(t) = frac(sum_(i=2)^5 V_i^b(t) - sum_(i=2)^5 V_i^a(t), sum_(i=2)^5 V_i^b(t) + sum_(i=2)^5 V_i^a(t) + epsilon) $

相比OVI（1–5档合计），OBI着重捕捉"耐心资金"的方向偏好，提供与OVI部分正交的信息维度。

== 1.4　岭回归模型的数学原理与选择依据

=== 1.4.1　数学原理

岭回归（Ridge Regression）是在普通最小二乘（OLS）的损失函数中引入$L_2$正则化项的线性回归方法，优化目标为：

$ min_(bold(w), b) ||bold(y) - bold(X)bold(w) - b||_2^2 + alpha ||bold(w)||_2^2 $

其中$bold(X) in RR^(n times p)$为特征矩阵，$bold(y) in RR^n$为标签向量，$bold(w) in RR^p$为特征权重，$alpha > 0$为正则化强度超参数。与OLS不同，Ridge存在封闭形式解析解：

$ hat(bold(w)) = (bold(X)^top bold(X) + alpha bold(I))^(-1) bold(X)^top bold(y) $

正则化项$alpha bold(I)$的核心作用是将$bold(X)^top bold(X)$的条件数从潜在无穷大压缩至有限值，使逆矩阵数值稳定存在，无需依赖迭代优化。从贝叶斯视角，Ridge等价于对权重施加零均值高斯先验$bold(w) tilde cal(N)(bold(0), frac(sigma^2, alpha) bold(I))$，正则化强度$alpha$控制先验精度。

正则化对权重向量施加收缩：随$alpha$增大，$hat(bold(w))$整体趋向零，方差（过拟合风险）降低，偏差上升，形成经典偏差-方差权衡（Bias-Variance Tradeoff）。本文根据子模型特性差异化设定$alpha$：多因子综合模型$alpha=150$，OVI纯净低维模型$alpha=10$（维度低、共线性弱，无需强压缩），高维交互特征模型$alpha=1000$（乘积项方差大，需强正则）。

=== 1.4.2　岭回归在本任务中的优势

#hei[应对高度多重共线性。]本文59个特征中，多类OVI脉冲（OVI\_p15、OVI\_p30、OVI\_p60）彼此高度相关（$rho > 0.8$），TI脉冲族同理。在存在多重共线性时，OLS估计量方差趋向无穷大，系数估计极不稳定，微小的数据扰动会导致系数剧烈震荡。岭回归通过$L_2$正则化显式压缩相关特征的系数幅度，在保留全部特征信息的同时大幅降低估计方差，获得更稳定的泛化性能。

#hei[在小样本下的强泛化性。]本任务训练集规模约$n = 110000 tilde 138000$行（4天训练）、特征维度$p = 59$，从数据量看并不小，但有效独立样本数远低于行数（相邻Tick强自相关，有效样本量约为行数的$1\/60 tilde 1\/100$）。在有效样本受限的条件下，参数数量较少的正则化线性模型泛化能力优于高容量非线性模型。实验对比验证了这一点：Ridge（$alpha=150$）在五折CV上的均值IC（0.3302）显著优于等规模LightGBM（IC约0.22），原因正是后者在有效样本不足时产生严重过拟合。

#hei[严格线性预测路径，零冷启动风险。]岭回归推理仅需一次向量内积$hat(y)_i(t) = bold(w)_i^top bold(x)(t) + b_i$，时间复杂度$O(p)$，完全可在500ms快照间隔内实时完成。更重要的是，其解析解在训练集上即确定全部参数，无迭代预热、无在线参数更新，消除了在线学习算法在日初冷启动期参数不稳定的风险。这与本任务每天重新以上日数据预测的场景高度契合。

#hei[相比Lasso的连续可微优势。]Lasso（$L_1$正则化）在有效特征选择场景下有优势，但会将部分相关特征系数直接归零，可能丢失组合预测力。Ridge对所有特征施加连续可微的二次惩罚，保留全部59个因子的联合贡献，通过集成不同特征子集的多个子模型实现隐式"特征选择"，兼顾完整性与稳定性。

== 1.5　动态集成机制的选择依据与数学推导

由于在线预测时可以计算五分钟前的Return5min指标，我们采取了在线评估各个模型在当日的历史预测效果并动态调整模型权重的方式。我们发现单一模型在不同时间段的表现存在显著差异，且模型间的IC相关性较低（平均相关系数约0.3），表明不同模型在不同市场状态下具有互补的预测能力。基于此，我们设计了基于滚动IC的Softmax动态集成机制，核心思想是：根据每个子模型在最近历史窗口内的预测表现（滚动IC）动态分配权重，优胜劣汰，适应市场状态变化。为此我们引入了一个稳定模型（MTC）作为基准，在窗口积累不足时享有2倍先验权重，确保集成的稳健性；27个特化模型则采用零预热机制，初始权重为0，只有在积累足够IC历史后才参与集成，防止噪声IC污染早期权重估计。

设第 $i$ 个子模型在滑动窗口 $[t - W, t - d)$ 内的滚动IC为：

$ "IC"_i(t) = "Pearson"\( {hat(y)_i(s)}_(s=t-W)^(t-d),\ {y(s)}_(s=t-W)^(t-d) \) $

其中 $W = 600$（约5分钟），$d = 600$（标签滞后，确保 $y(t-d)$ 在 $t$ 时刻已知）。

EWMA平滑抑制IC估计的短期噪声，半衰期约为 $T_(1/2) = -ln 2 / ln(1-beta) approx 99$ 步（$beta=0.007$）：

$ tilde("IC")_i(t) = (1-beta) dot tilde("IC")_i(t - Delta) + beta dot "IC"_i(t), quad Delta = 15 $

集成权重通过带温度的Softmax归一化（$tau = 17$）：

$ w_i(t) = frac(exp(tilde("IC")_i(t) dot tau), sum_(j=1)^N exp(tilde("IC")_j(t) dot tau)) $

温度参数 $tau$ 控制权重集中程度；最终预测：

$ hat(y)(t) = sum_(i=1)^N w_i(t) dot hat(y)_i(t) $

子模型分为两类：稳定模型（MTC，1个）在窗口积累不足时享有2倍先验权重；27个Niche模型预热期初始权重为0（零预热机制），积累足够IC历史后才参与集成，防止噪声IC污染早期权重估计。

//------------------------------

= 测试流程

== 2.1　特征和标签选取

=== 2.1.1　标签选取

模型的预测目标为股票E的未来五分钟中间价收益率。我们在训练时直接把它作为标签，没有进行任何处理，保持原始收益率形式以便进行IC评估。

=== 2.1.2　特征选取与筛选

我们基于1.3所述的理论基础提出了多个方位的特征，并采用以下两个标准进行筛选，确保特征的有效性：

(1) #hei[特征与标签的IC]——稳健模型的候选特征须在全部5个测试日上IC符号一致，特化模型的部分特征也必须与标签有较高的相关性，以保证特征对于标签预测的有效性。

(2) #hei[正交互补]——新增特征与现有特征相关系数须 $< 0.7$，以防止重复引入冗余特征。

=== 2.1.3　特征体系概览

经过上述原则的筛选，我们最终构建了一个包含59个特征的多维度特征体系，涵盖了8大类，分别捕捉不同层次和维度的市场行为信号，为模型提供丰富且有效的预测信息。

#figure(
  image("figures/fig3_feature_categories.png", width: 80%),
  caption: [特征体系分类分布（共59个特征，8大类）],
) <fig3>

各类特征的数学定义与经济学含义如下：

(1) #hei[成交量失衡（TI）脉冲（8个）：] $"TI_p"k = overline("TI")_k - overline("TI")_600$（$k in {15,30,40,60}$），捕捉成交方向的短期偏离；另包含EMA版本 $"TI_ep"k$。

(2) #hei[委托量失衡（OVI）脉冲（5个）：] 如第1.4.1节定义，均值IC≈0.26，为最强单因子。

(3) #hei[委托/成交笔数失衡（4个）：] 笔数层面的方向信号ONI（Order Number Imbalance）和TNI（Trade Number Imbalance），与OVI量纲不同，提供正交信息。

(4) #hei[板块联动因子（7个）：] A/B/C/D四只股票的截面均值信号，包含板块OBI、板块TI脉冲、板块OVI脉冲等，捕捉系统性板块动量效应。

(5) #hei[日内时间特征（2个）：] `aft_13800`（下午标志，$t >= 13800$s）和 `aft_12000`，利用下午市场反转效应。

(6) #hei[滞后价格收益特征（12个）：] 包括E股和板块过去5分钟已实现收益（均值回复信号），以及E股30/60/120/300/600/900 Tick中间价收益率。

(7) #hei[委托簿深度与流量（7个）：] 深层委托簿失衡（OBI，2–5档）、买卖价差脉冲、大单成交失衡、日内累计成交流量失衡，捕捉"耐心资金"和机构行为。

(8) #hei[交互与高级信号（14个）：] 波动率条件化OVI（$"OVI" times |r|$）、截面异质OVI（e_sect_lag_gap，IC均值=0.203）、价格动量加速度（$r_15 - r_30$）、OVI平方项及多重两两交互（如 $"OVI" times "TBV"$），为线性Ridge模型提供非线性信息。

== 2.2　特征预处理

=== 2.2.1　特征标准化

Ridge回归对特征量纲敏感。训练时各子模型在训练集上做 $z$-score 标准化（$hat(x) = (x - mu) \/ sigma$）；在线推理时使用训练阶段保存的 $mu, sigma$ 做相同变换，均值和标准差随模型系数存入 `models.pkl`。

=== 2.2.2　极端值裁剪

对收益率类特征实施截断：

- 长窗口收益率（$>= 120$ Tick）：$x arrow.l "clip"(x, -0.1, 0.1)$
- 短窗口收益率（30/60 Tick）：$x arrow.l "clip"(x, -0.05, 0.05)$
- 交互特征（乘积项）：额外乘以归一化系数后裁剪至 $[-0.5, 0.5]$，防止乘积项方差过大。

=== 2.2.3　冷启动处理

日初前 $k$ 个Tick（$k$ 为特征滞后长度）无历史数据，使用当日有效值的运行均值填充，而非用 $0$ 填充。用 $0$ 填充会在冷启动期制造可学习但不泛化的假信号；均值填充可避免模型学到虚假冷启动规律 @fillna。

== 2.3　训练集和交叉验证集的设置

=== 2.3.1　五折按日交叉验证（LODO-CV）

由于训练数据仅5天（Fold数等于日数），采用Leave-One-Day-Out交叉验证（LODO-CV）@lodo_cv：每折将一天作为测试集，其余四天作为训练集，共5折。优点：最大化训练数据利用率（每折使用4天训练）；测试日相互独立，IC均值为无偏估计；与竞赛评分机制（按天取IC均值）完全一致。实现中以 `GroupKFold(n_splits=5)` 配合 `Day` 列为分组标识，确保同一天数据不会同时出现在训练集和测试集。

=== 2.3.2　嵌套盲测验证

为定量评估调参过拟合程度，还实施了嵌套盲测（Nested Blind Test）：
- #hei[外层（盲测IC）：] 用其余4天训练，在保留天完整预测，验证代码层面无数据泄露。
- #hei[内层（调参IC）：] 在4天训练集内部运行4折CV，是调参时实际参考的数字，含对4天数据的过拟合。
- #hei[内-外gap：] Iter20模型内外gap约为 $-0.019$（外层略高），证明调参过拟合可控。

== 2.4　模型训练和参数设定

=== 2.4.1　Ridge回归子模型

岭回归的数学原理与选择依据已在第1.3节详细阐述。每个子模型优化目标为$min_{bold(w), b} ||bold(y) - bold(X)bold(w) - b||_2^2 + alpha ||bold(w)||_2^2$，解析解$hat(bold(w)) = (bold(X)^top bold(X) + alpha bold(I))^(-1) bold(X)^top bold(y)$在训练阶段一次求解后保存至`models.pkl`，在线推理仅需$O(p)$向量内积。正则化系数$alpha$按子模型特性差异化设定：多因子综合模型$alpha=150$，OVI纯净低维模型$alpha=10$，高维交互特征模型$alpha=1000$，详见第1.3.1节的偏差-方差分析 @ridge。

=== 2.4.2　28个子模型的设计

28个子模型覆盖多种特征子集和时间段组合。1个稳定模型（MTC）负责全时段稳健预测，作为预热期基准；27个Niche模型针对特定信号组合和时段，在各自擅长的市场条件下表现突出。

#figure(
  kind: table,
  caption: [子模型分类概览],
  table(
    columns: (auto, auto, auto, auto),
    align: (left, center, center, left),
    stroke: 0.5pt,
    [#hei[模型类别]], [#hei[数量]], [#hei[正则化 $alpha$]], [#hei[特征重点]],
    [稳定（Stable）], [1], [150], [TI纯净 + 时间特征（MTC）],
    [OVI纯净niche], [4], [10–150], [OVI脉冲系列为主要输入],
    [截面反转niche], [3], [150], [`sect_ret_lag` + `e_ret_lag` + `e_sect_lag_gap`],
    [非线性niche], [4], [150–1000], [`spread_wt_ovi`、`ovi_sq`、交互特征族],
    [深度/流量niche], [6], [150], [`obi_deep_p15`、`cum_flow_imb`、`book_pres_pulse`],
    [综合多因子niche], [10], [150–500], [多信号组合],
  )
) <tab1>

=== 2.4.3　迭代优化历程

如图2所示，模型经历20轮迭代优化，IC从0.147稳步提升至0.3302：

#figure(
  image("figures/fig1_ic_evolution.png", width: 95%),
  caption: [模型IC迭代演进曲线（Iter0→Iter20）],
) <fig1>

主要里程碑：Iter0–2建立LODO-CV评估体系，IC从0.147提升至0.224；Iter3–8引入动态集成机制，IC提升至0.279；Iter9–12引入累计流量失衡和交互特征，IC首次突破0.30；Iter13–19加入动量加速度、波动率条件化、截面异质OVI等高级信号，IC稳步提升至0.313，ICIR达7.82；Iter20采用LOO剪枝，54个模型精简为28个，IC跃升至0.330，ICIR从7.82飙升至12.59（+61%）。

=== 2.4.4　集成参数设定

#figure(
  kind: table,
  caption: [动态集成参数汇总],
  table(
    columns: (auto, auto, auto),
    align: (left, center, left),
    stroke: 0.5pt,
    [#hei[参数]], [#hei[值]], [#hei[含义]],
    [`ENSEMBLE_WINDOW`], [600], [滚动IC计算窗口（约5分钟）],
    [`ENSEMBLE_TEMP`], [17], [Softmax温度（权重集中程度）],
    [`ENSEMBLE_FLOOR`], [0], [权重下界（允许权重降至0）],
    [`RETURN_DELAY`], [600], [IC计算所需标签滞后（Tick）],
    [`ENSEMBLE_UPDATE_FREQ`], [15], [权重更新频率（每15 Tick）],
    [`ENSEMBLE_EWMA_BETA`], [0.007], [IC平滑系数（半衰期约99 Tick）],
    [`STABLE_PRIOR`], [2.0], [稳定模型预热期先验权重倍数],
    [`NICHE_INIT_WEIGHT`], [0], [Niche模型预热期初始权重],
  )
) <tab2>

== 2.5　测试结果展示和模型评价

=== 2.5.1　逐日IC结果

图3展示了最终模型（Iter20）的五折CV逐日IC：

#figure(
  image("figures/fig2_final_cv.png", width: 85%),
  caption: [Iter20最终模型五折CV逐日IC（均值=0.330，ICIR=12.59）],
) <fig2>

五天测试IC分别为：Day1=0.326、Day2=0.363、Day3=0.338、Day4=0.284、Day5=0.341，均值IC=0.3302，标准差=0.026，ICIR=12.59。Day4（低波动趋势日）IC最低（0.284），与AMH预测一致——微观结构信号在趋势性行情中预测力相对弱。五天IC标准差仅0.026，体现了极高的跨日稳定性。

=== 2.5.2　基线对比

图4展示了从Iter1（6特征线性基线）到Iter20（28模型动态集成）的逐日IC提升：

#figure(
  image("figures/fig4_iter_comparison.png", width: 90%),
  caption: [Iter1基线 vs Iter20最终模型逐日IC对比],
) <fig4>

相比基线，Iter20在每一天上均取得显著提升：Day1提升72.5%（0.189→0.326），Day3提升134.7%（0.144→0.338），Day4提升129.0%（0.124→0.284）——弱信号日（Day1/3/4）的改善尤为突出，体现了多因子集成对多样化市场状态的覆盖能力。

=== 2.5.3　动态集成增益验证

图5展示了嵌套盲测中动态集成相对稳定等权基准的增益：

#figure(
  image("figures/fig5_ensemble_gain.png", width: 95%),
  caption: [动态集成 vs 稳定等权基准的IC增益（嵌套盲测）],
) <fig5>

动态集成在所有5天上均优于等权基准，平均增益+0.051，验证了滚动IC-Softmax机制的有效性 @softmax_ensemble。增益最大的是Day5（+0.104），该天Niche模型的滚动IC高度分化，动态权重精准识别了强信号模型。

=== 2.5.4　综合模型评价

#figure(
  kind: table,
  caption: [最终模型（Iter20）综合评价指标],
  table(
    columns: (auto, auto, auto),
    align: (left, center, left),
    stroke: 0.5pt,
    [#hei[评价维度]], [#hei[指标值]], [#hei[说明]],
    [五折CV均值IC], [0.3302], [主要竞赛指标，out-of-sample估计],
    [五折CV IC标准差], [0.026], [跨日稳定性，越低越好],
    [ICIR（IC/Std）], [12.59], [风险调整后IC，历史最高],
    [Penalized（M$-$0.5S）], [0.317], [惩罚性指标，兼顾均值与方差],
    [内层4折IC], [0.311], [调参参考值（含轻微调参偏差）],
    [内-外gap], [$-$0.019], [负值=外层略高，调参过拟合可控],
    [集成增益（vs等权）], [+0.051], [动态集成相对等权基准提升],
    [模型复杂度], [28子模型，59特征], [相比Iter19（54×59）更简洁],
  )
) <tab3>

本模型在应用前景方面也具有较强价值：(1) 预测值 $hat(y)(t)$ 可直接用于超短期仓位决策，在合理交易成本假设下具备实盘部署价值；(2) 做市商可利用模型预测优化挂单策略，降低逆向选择风险；(3) 框架在多标的、跨资产场景均可扩展；(4) 28个子模型的实时权重分布本身是市场状态的量化刻画，可为风险管理提供实时的市场状态指示器。

//------------------------------

= 参考文献

#bibliography("references.bib", style: "gb-7714-2015-numeric", title: none)
